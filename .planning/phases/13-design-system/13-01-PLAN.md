---
phase: 13-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/package.json
  - website/components.json
  - website/components/ui/button.tsx
  - website/components/ui/input.tsx
  - website/components/ui/label.tsx
  - website/components/ui/badge.tsx
  - website/components/ui/card.tsx
autonomous: true

must_haves:
  truths:
    - "Button component renders with 48px height default (h-12)"
    - "Input component has 48px touch target with RTL text alignment"
    - "Badge renders inline with RTL-safe spacing"
    - "Card component uses logical properties for padding"
  artifacts:
    - path: "website/components/ui/button.tsx"
      provides: "Primary interactive element with variants"
      contains: "buttonVariants"
    - path: "website/components/ui/input.tsx"
      provides: "Form text input with RTL support"
      contains: "Input"
    - path: "website/components/ui/badge.tsx"
      provides: "Status indicator component"
      contains: "badgeVariants"
    - path: "website/components/ui/card.tsx"
      provides: "Content container component"
      contains: "Card"
    - path: "website/components.json"
      provides: "shadcn/ui configuration"
      contains: "aliases"
  key_links:
    - from: "website/components/ui/button.tsx"
      to: "class-variance-authority"
      via: "cva import"
      pattern: "from \"class-variance-authority\""
    - from: "website/components/ui/*.tsx"
      to: "website/lib/utils.ts"
      via: "cn() import"
      pattern: "from \"@/lib/utils\""
---

<objective>
Install shadcn/ui dependencies and generate core atomic components with RTL support.

Purpose: Establish the foundation for all UI components with type-safe variants, RTL compatibility, and 48px touch targets.
Output: Five shadcn/ui components (button, input, label, badge, card) configured for Hebrew RTL and mobile touch targets.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-design-system/13-RESEARCH.md
@.planning/phases/13-design-system/13-CONTEXT.md
@website/lib/utils.ts
@website/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and initialize shadcn/ui</name>
  <files>website/package.json, website/components.json</files>
  <action>
Install required dependencies:
```bash
cd website && pnpm add class-variance-authority @radix-ui/react-slot lucide-react
```

Initialize shadcn/ui with RTL support. Run the init command and configure:
- Style: default
- Base color: neutral (we'll customize in Plan 02)
- CSS variables: yes
- Tailwind CSS path: app/globals.css
- Components directory: components/ui
- Utils path: lib/utils.ts (already exists with cn())

```bash
cd website && pnpm dlx shadcn@latest init
```

When prompted, accept defaults but ensure:
- aliases.components: "@/components"
- aliases.utils: "@/lib/utils"

Note: Do NOT install next-themes here - that's Plan 02. shadcn init may ask about it; decline or skip.
  </action>
  <verify>
- `cat website/package.json | grep class-variance-authority` shows version
- `cat website/components.json` exists and shows aliases configuration
- `pnpm run build` in website/ succeeds
  </verify>
  <done>
- class-variance-authority, @radix-ui/react-slot, lucide-react in dependencies
- components.json created with correct aliases
- Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and customize shadcn/ui atomic components</name>
  <files>website/components/ui/button.tsx, website/components/ui/input.tsx, website/components/ui/label.tsx, website/components/ui/badge.tsx, website/components/ui/card.tsx</files>
  <action>
Add shadcn/ui components:
```bash
cd website && pnpm dlx shadcn@latest add button input label badge card
```

After generation, customize each component for Findo requirements:

**button.tsx customizations:**
1. Change default size from h-10 to h-12 (48px touch target per MOBILE-02)
2. Add loading variant with shimmer animation (per CONTEXT.md - no spinner)
3. Use rounded-lg (8-12px radius per CONTEXT.md)
4. Ensure focus-visible ring styling

Update buttonVariants size.default to:
```typescript
size: {
  default: "h-12 px-6 py-3 text-base",  // 48px touch target
  sm: "h-10 px-4 text-sm",
  lg: "h-14 px-8 text-lg",
  icon: "h-12 w-12",  // 48px square
}
```

Add loading variant to buttonVariants:
```typescript
loading: {
  true: "pointer-events-none animate-shimmer bg-gradient-to-r from-[hsl(var(--primary))] via-[hsl(var(--primary)/0.8)] to-[hsl(var(--primary))] bg-[length:200%_100%]",
  false: "",
}
```

Add loading prop to ButtonProps interface and pass to variants.

**input.tsx customizations:**
1. Add h-12 height for 48px touch target
2. Ensure text-start for RTL alignment
3. Add focus-visible ring styling

**label.tsx:** Keep as-is (shadcn default is accessible)

**badge.tsx customizations:**
1. Use logical properties (ps-*, pe-*) instead of px-*
2. Keep text sizes accessible

**card.tsx customizations:**
1. Use logical properties for padding (ps-6 pe-6 instead of px-6)
2. Add rounded-lg per CONTEXT.md styling
  </action>
  <verify>
- All 5 files exist in website/components/ui/
- `grep "h-12" website/components/ui/button.tsx` shows 48px default
- `grep "animate-shimmer" website/components/ui/button.tsx` shows loading variant
- `pnpm run build` in website/ succeeds
  </verify>
  <done>
- button.tsx has h-12 default, loading shimmer variant, rounded-lg
- input.tsx has h-12 height with RTL text-start
- badge.tsx uses logical properties
- card.tsx uses logical properties with rounded-lg
- All components import cn() from @/lib/utils
- Build passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Add shimmer animation to globals.css</name>
  <files>website/app/globals.css</files>
  <action>
Add the shimmer keyframes animation to globals.css for the button loading state.

Add after the @theme block:
```css
/* Shimmer animation for loading states (no spinner per CONTEXT.md) */
@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

.animate-shimmer {
  animation: shimmer 2s ease-in-out infinite;
}
```

This provides the loading shimmer effect specified in CONTEXT.md ("Loading state: button pulses/shimmers, no spinner").
  </action>
  <verify>
- `grep "animate-shimmer" website/app/globals.css` shows the class
- `grep "@keyframes shimmer" website/app/globals.css` shows the animation
- `pnpm run build` in website/ succeeds
  </verify>
  <done>
- globals.css contains shimmer keyframes
- .animate-shimmer class defined
- Button loading state has visual shimmer effect
- Build passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd website && pnpm run build` - must succeed
2. All 5 component files exist in components/ui/
3. Button has 48px default height and loading shimmer
4. Input has 48px height
5. Card uses logical properties
6. components.json properly configured
</verification>

<success_criteria>
- shadcn/ui initialized with components.json
- 5 atomic components (button, input, label, badge, card) generated and customized
- Button touch target is 48px (h-12) by default
- Button loading state uses shimmer animation (no spinner)
- All components use cn() from @/lib/utils
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-system/13-01-SUMMARY.md`
</output>
