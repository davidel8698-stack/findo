---
phase: 13-design-system
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/ui/button.tsx
  - website/providers/ThemeProvider.tsx
  - website/components/molecules/StatItem.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All button sizes meet 48px WCAG touch target requirement"
    - "Page loads in dark mode by default without system preference override"
    - "StatItem animates with stagger effect when inside StaggerContainer"
  artifacts:
    - path: "website/components/ui/button.tsx"
      provides: "Button with 48px minimum height for all sizes"
      contains: "sm: \"h-12"
    - path: "website/providers/ThemeProvider.tsx"
      provides: "Dark mode by default without enableSystem"
    - path: "website/components/molecules/StatItem.tsx"
      provides: "Motion-enabled stat item for stagger animations"
      contains: "m.div"
  key_links:
    - from: "website/components/molecules/StatItem.tsx"
      to: "website/components/motion/variants.ts"
      via: "fadeInUp variant import"
      pattern: "import.*fadeInUp.*from"
---

<objective>
Fix 3 UAT issues from Phase 13 verification: button touch targets, dark mode default, and stagger animation.

Purpose: Close gaps identified during user acceptance testing to achieve full WCAG compliance and design system functionality.
Output: Three single-line fixes that resolve all major issues.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-design-system/13-UAT.md
@website/components/ui/button.tsx
@website/providers/ThemeProvider.tsx
@website/components/molecules/StatItem.tsx
@website/components/motion/variants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix button sm size to 48px touch target</name>
  <files>website/components/ui/button.tsx</files>
  <action>
Change the `sm` size variant from h-10 to h-12 for WCAG touch target compliance.

Line 24, change:
```
sm: "h-10 px-4 text-sm",
```
to:
```
sm: "h-12 px-4 text-sm",
```

This ensures all button sizes meet the 48px minimum touch target requirement (MOBILE-02).
  </action>
  <verify>Grep for `sm: "h-12` in button.tsx</verify>
  <done>All button size variants use h-12 or larger (48px+ touch targets)</done>
</task>

<task type="auto">
  <name>Task 2: Remove enableSystem from ThemeProvider</name>
  <files>website/providers/ThemeProvider.tsx</files>
  <action>
Remove the `enableSystem` prop from NextThemesProvider to prevent system light mode preference from overriding the defaultTheme="dark" setting.

Line 14, delete:
```
enableSystem
```

The ThemeProvider should only have: attribute="class", defaultTheme="dark", disableTransitionOnChange
  </action>
  <verify>Grep confirms no `enableSystem` in ThemeProvider.tsx</verify>
  <done>Page always loads in dark mode regardless of system preference</done>
</task>

<task type="auto">
  <name>Task 3: Add motion wrapper to StatItem</name>
  <files>website/components/molecules/StatItem.tsx</files>
  <action>
Wrap StatItem content in m.div with fadeInUp variants so it animates when inside a StaggerContainer.

1. Add import at top:
```typescript
import { m } from "motion/react";
import { fadeInUp } from "@/components/motion/variants";
```

2. Change the outer `<div` to `<m.div` and add variants prop:
```typescript
<m.div
  variants={fadeInUp}
  className={cn(
```

3. Change closing `</div>` to `</m.div>`

The component should now be a motion component that responds to parent StaggerContainer animation orchestration.
  </action>
  <verify>Grep for `m.div` and `variants={fadeInUp}` in StatItem.tsx</verify>
  <done>StatItem animates in sequence when rendered inside StaggerContainer</done>
</task>

</tasks>

<verification>
1. Build passes: `cd website && npm run build`
2. No TypeScript errors
3. Grep confirms all fixes applied:
   - `grep "sm.*h-12" website/components/ui/button.tsx`
   - `grep -v "enableSystem" website/providers/ThemeProvider.tsx`
   - `grep "m.div" website/components/molecules/StatItem.tsx`
</verification>

<success_criteria>
- All 3 UAT gaps closed
- Build succeeds without errors
- Button sm size is 48px (h-12)
- ThemeProvider has no enableSystem prop
- StatItem uses m.div with fadeInUp variants
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-system/13-06-SUMMARY.md`
</output>
