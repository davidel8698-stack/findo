---
phase: 14-hero-first-impression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/hero/Hero.tsx
  - website/components/sections/hero/HeroContent.tsx
  - website/components/sections/hero/PhoneMockup.tsx
  - website/components/sections/hero/index.ts
autonomous: true

must_haves:
  truths:
    - "Hero section renders with RTL-native layout (content right, visual left)"
    - "Headline and subheadline display in Hebrew with correct typography"
    - "Phone mockup provides container for activity feed cards"
    - "Hero takes full viewport height on desktop, adapts on mobile"
  artifacts:
    - path: "website/components/sections/hero/Hero.tsx"
      provides: "Main hero section with grid layout"
      min_lines: 30
    - path: "website/components/sections/hero/HeroContent.tsx"
      provides: "Headline, subheadline, CTA area"
      min_lines: 40
    - path: "website/components/sections/hero/PhoneMockup.tsx"
      provides: "CSS phone frame for activity cards"
      min_lines: 25
  key_links:
    - from: "website/components/sections/hero/Hero.tsx"
      to: "HeroContent, PhoneMockup"
      via: "component composition"
      pattern: "import.*HeroContent|PhoneMockup"
---

<objective>
Create the hero section layout with RTL-native structure, headline/subheadline content, and phone mockup component.

Purpose: Establish the above-fold visual hierarchy with Hebrew content displayed correctly in RTL layout. The phone mockup will serve as the container for the animated activity feed.

Output: Hero section components with proper layout, placeholder for activity feed, and Hebrew messaging that speaks to the target audience's pain.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-hero-first-impression/14-CONTEXT.md
@.planning/phases/14-hero-first-impression/14-RESEARCH.md

# Existing components to use
@website/components/ui/button.tsx
@website/components/molecules/CTAGroup.tsx
@website/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero section folder structure and PhoneMockup component</name>
  <files>
    website/components/sections/hero/PhoneMockup.tsx
    website/components/sections/hero/index.ts
  </files>
  <action>
Create the sections/hero folder structure with:

1. PhoneMockup.tsx - Pure CSS phone frame using Tailwind:
   - Modern phone shape with rounded corners (2.5rem border-radius)
   - 12px border to simulate phone frame
   - Notch at top (centered, rounded bottom)
   - Side buttons (volume on start side, power on end side - RTL aware with logical properties)
   - Inner screen area with rounded corners and overflow hidden
   - Responsive sizing: h-[500px] w-[240px] on mobile, md:h-[600px] md:w-[290px] on desktop
   - Accept children prop for content (activity cards)
   - Use card/card-foreground colors for theming
   - Add subtle shadow-xl for depth

2. index.ts - Barrel export for all hero components:
   - Export PhoneMockup
   - Prepare exports for Hero, HeroContent (will add in next task)

Use "use client" directive for PhoneMockup (will contain animated children).
Use logical CSS properties (start/end) for RTL support.
  </action>
  <verify>
TypeScript compiles without errors: `cd website && npx tsc --noEmit`
PhoneMockup renders correctly in isolation
  </verify>
  <done>
PhoneMockup component exists with CSS phone frame styling, responsive sizing, and RTL-aware side buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HeroContent component with headline and CTA</name>
  <files>
    website/components/sections/hero/HeroContent.tsx
  </files>
  <action>
Create HeroContent.tsx with the hero messaging:

1. Headline (h1):
   - Problem-focused Hebrew headline under 8 words
   - Recommended: "נמאס לך לרדוף אחרי לקוחות?" (Tired of chasing customers?)
   - Alternative: "העסק שלך עובד. אתה לא צריך." (Your business works. You don't have to.)
   - Use text-4xl md:text-5xl lg:text-6xl font-bold
   - text-foreground for maximum contrast

2. Subheadline (p):
   - Explains the mechanism in one sentence
   - "Findo מנהל את הגוגל ביזנס שלך באופן אוטומטי - משיב לביקורות, מפרסם תוכן, ולוכד לידים בוואטסאפ. אתה לא עושה כלום."
   - Use text-lg md:text-xl text-muted-foreground
   - max-w-lg to control line length

3. CTA area:
   - Use existing CTAGroup component
   - Primary: "התחל בחינם" with ArrowLeft icon (RTL arrow direction)
   - Secondary: "איך זה עובד?"
   - Add mt-8 spacing

4. Layout:
   - text-center on mobile, lg:text-start on desktop
   - Proper spacing between elements (space-y-4 or space-y-6)

Import CTAGroup from @/components/molecules, Icon from @/components/atoms.
Use "use client" directive.
  </action>
  <verify>
TypeScript compiles: `cd website && npx tsc --noEmit`
Hebrew text renders correctly with RTL alignment
  </verify>
  <done>
HeroContent component renders headline under 8 words, subheadline explaining mechanism, and CTA group with value-focused text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Hero section with RTL-native grid layout</name>
  <files>
    website/components/sections/hero/Hero.tsx
    website/components/sections/hero/index.ts
  </files>
  <action>
Create Hero.tsx as the main section component:

1. Section wrapper:
   - min-h-[100dvh] for full viewport height (use dvh for mobile safety)
   - flex items-center for vertical centering
   - py-16 md:py-20 for breathing room

2. Container:
   - Use container class for max-width and padding
   - mx-auto for centering

3. Grid layout (RTL-native):
   - grid grid-cols-1 lg:grid-cols-2
   - gap-8 lg:gap-12
   - items-center for vertical alignment

4. Content order (critical for RTL):
   - HeroContent: order-2 lg:order-1 (appears first in DOM for SEO, displays on RIGHT in RTL desktop)
   - PhoneMockup area: order-1 lg:order-2 (appears second in DOM, displays on LEFT in RTL desktop)
   - On mobile, visual appears first (order-1), then content

5. PhoneMockup container:
   - flex justify-center lg:justify-start
   - Render PhoneMockup with placeholder content for now (empty or simple text)
   - Activity cards will be added in Plan 02

Update index.ts to export Hero and HeroContent.

Import section as default export: `export { Hero as default } from "./Hero"`
  </action>
  <verify>
`cd website && npx tsc --noEmit` passes
Hero section has correct grid layout with content on right in RTL
PhoneMockup appears on left side in desktop RTL view
  </verify>
  <done>
Hero section renders with RTL-native grid (content right, visual left on desktop), full viewport height, and proper responsive behavior.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd website && npx tsc --noEmit` - TypeScript compiles
2. `cd website && npm run build` - Production build succeeds
3. Visual check: Hero layout correct in RTL (content right, phone left)
4. Visual check: Hebrew headline readable at all breakpoints
5. Visual check: Phone mockup renders with correct proportions
</verification>

<success_criteria>
- Hero section renders at full viewport height
- RTL layout places content on right, visual on left (desktop)
- Hebrew headline is under 8 words and problem-focused
- Subheadline explains the mechanism
- CTA uses existing CTAGroup with value-focused text
- PhoneMockup provides styled container for activity cards
- All components use logical CSS properties for RTL
</success_criteria>

<output>
After completion, create `.planning/phases/14-hero-first-impression/14-01-SUMMARY.md`
</output>
