---
phase: 14-hero-first-impression
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - website/components/sections/hero/TrustSignal.tsx
  - website/components/sections/hero/StickyCtaBar.tsx
  - website/components/sections/hero/Hero.tsx
  - website/components/sections/hero/HeroContent.tsx
  - website/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Trust signal shows specific customer count below CTA"
    - "Sticky CTA bar appears on mobile after scrolling past hero"
    - "ActivityFeed renders inside PhoneMockup in Hero section"
    - "Homepage renders complete hero section"
    - "Primary CTA visible without scrolling"
  artifacts:
    - path: "website/components/sections/hero/TrustSignal.tsx"
      provides: "Subtle trust indicator with specific number"
      min_lines: 15
    - path: "website/components/sections/hero/StickyCtaBar.tsx"
      provides: "Mobile sticky CTA that appears on scroll"
      min_lines: 30
      contains: "useEffect"
    - path: "website/app/page.tsx"
      provides: "Homepage with Hero section"
      contains: "Hero"
  key_links:
    - from: "website/components/sections/hero/Hero.tsx"
      to: "ActivityFeed"
      via: "composition inside PhoneMockup"
      pattern: "<ActivityFeed"
    - from: "website/app/page.tsx"
      to: "Hero"
      via: "section import"
      pattern: "import.*Hero.*from.*sections/hero"
---

<objective>
Integrate all hero components, add trust signal and sticky CTA, and wire hero section to homepage.

Purpose: Complete the above-fold experience by connecting all hero pieces: activity feed inside phone mockup, trust signal for credibility, and sticky CTA for mobile users. This plan makes the hero section live on the homepage.

Output: Fully functional hero section on homepage with trust signal, sticky mobile CTA, and animated activity feed.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-hero-first-impression/14-CONTEXT.md
@.planning/phases/14-hero-first-impression/14-RESEARCH.md

# Will exist after Plan 01 and 02
# @website/components/sections/hero/Hero.tsx
# @website/components/sections/hero/ActivityFeed.tsx

# Existing components
@website/components/ui/button.tsx
@website/components/molecules/CTAGroup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrustSignal component</name>
  <files>
    website/components/sections/hero/TrustSignal.tsx
  </files>
  <action>
Create TrustSignal.tsx for above-fold social proof:

1. Props interface:
   ```typescript
   interface TrustSignalProps {
     value: string;  // e.g., "573" - specific, not rounded
     label: string;  // e.g., "עסקים סומכים על Findo"
     className?: string;
   }
   ```

2. Styling (subtle, not competing with CTA):
   - flex items-center gap-2
   - text-muted-foreground text-sm
   - Value: font-bold text-foreground (stands out slightly)
   - Label: regular weight

3. Per research:
   - Use specific numbers ("573") not rounded ("500+")
   - Keep visually subdued
   - Place below CTA in HeroContent

No "use client" needed - purely presentational.
Import cn from @/lib/utils.
  </action>
  <verify>
`cd website && npx tsc --noEmit` passes
TrustSignal renders with subtle styling
  </verify>
  <done>
TrustSignal component displays specific customer count with subtle visual treatment that doesn't compete with CTA.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StickyCtaBar for mobile</name>
  <files>
    website/components/sections/hero/StickyCtaBar.tsx
  </files>
  <action>
Create StickyCtaBar.tsx for always-visible mobile CTA:

1. State management:
   - useState for visibility (isVisible)
   - Show after scrolling past 400px (hero CTA no longer visible)

2. Scroll listener with useEffect:
   ```typescript
   useEffect(() => {
     const handleScroll = () => {
       setIsVisible(window.scrollY > 400);
     };

     window.addEventListener("scroll", handleScroll, { passive: true });
     return () => window.removeEventListener("scroll", handleScroll);
   }, []);
   ```

3. Conditional render:
   - Return null if !isVisible

4. Styling:
   - fixed bottom-0 inset-x-0 z-50
   - p-4 for padding
   - bg-background/80 backdrop-blur-md for frosted effect
   - border-t border-border for separation
   - md:hidden (only show on mobile)
   - pb-safe-area-inset-bottom for iOS safe area (use pb-[env(safe-area-inset-bottom,1rem)])

5. Content:
   - Full-width Button with size="lg"
   - Same CTA text as hero: "התחל בחינם"
   - w-full className on button

Import Button from @/components/ui/button.
Use "use client" directive.
  </action>
  <verify>
`cd website && npx tsc --noEmit` passes
StickyCtaBar appears only on mobile after scrolling
  </verify>
  <done>
StickyCtaBar shows on mobile after scrolling 400px, with frosted glass effect and iOS safe area support.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate ActivityFeed into Hero and add TrustSignal</name>
  <files>
    website/components/sections/hero/Hero.tsx
    website/components/sections/hero/HeroContent.tsx
    website/components/sections/hero/index.ts
  </files>
  <action>
1. Update Hero.tsx:
   - Import ActivityFeed from "./ActivityFeed"
   - Replace placeholder in PhoneMockup with <ActivityFeed />
   - PhoneMockup wraps ActivityFeed: `<PhoneMockup><ActivityFeed /></PhoneMockup>`

2. Update HeroContent.tsx:
   - Import TrustSignal from "./TrustSignal"
   - Add TrustSignal below CTAGroup:
     ```tsx
     <TrustSignal
       value="573"
       label="עסקים סומכים על Findo"
       className="mt-6 justify-center lg:justify-start"
     />
     ```
   - Note: Using placeholder number - will be updated with real data later

3. Update index.ts:
   - Add exports for TrustSignal and StickyCtaBar:
     ```typescript
     export { TrustSignal } from "./TrustSignal";
     export { StickyCtaBar } from "./StickyCtaBar";
     ```

Ensure Hero is a client component ("use client") since it contains ActivityFeed with GSAP.
  </action>
  <verify>
`cd website && npx tsc --noEmit` passes
ActivityFeed renders inside PhoneMockup
TrustSignal appears below CTA
  </verify>
  <done>
Hero section integrates all components: HeroContent with TrustSignal, PhoneMockup with ActivityFeed. All exports updated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire Hero section to homepage</name>
  <files>
    website/app/page.tsx
  </files>
  <action>
Replace the current component showcase with the Hero section:

1. Import Hero and StickyCtaBar:
   ```typescript
   import Hero from "@/components/sections/hero";
   import { StickyCtaBar } from "@/components/sections/hero";
   ```

2. Create new page structure:
   ```tsx
   export default function HomePage() {
     return (
       <main className="min-h-screen bg-background text-foreground">
         <Hero />
         <StickyCtaBar />

         {/* Placeholder sections below hero */}
         <section className="container py-20">
           <p className="text-muted-foreground text-center">
             More sections coming in Phase 15+
           </p>
         </section>
       </main>
     );
   }
   ```

3. Keep "use client" directive if present (Hero uses client-side animation)

4. Remove old component showcase content - it served its purpose for Phase 13 verification

Note: The StickyCtaBar is placed at page level so it persists across all sections.
  </action>
  <verify>
`cd website && npm run dev` - dev server starts
Visit http://localhost:3000 - Hero section renders
Animation plays on load
Scroll down - sticky CTA appears on mobile viewport
  </verify>
  <done>
Homepage displays complete hero section with animated activity feed, trust signal, and mobile sticky CTA. Old showcase content removed.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd website && npx tsc --noEmit` - TypeScript compiles
2. `cd website && npm run build` - Production build succeeds
3. Homepage shows hero section with animated activity feed
4. Trust signal visible below CTA with specific number
5. Sticky CTA appears on mobile after scrolling
6. RTL layout correct (content right, phone left on desktop)
7. All hero exports available from @/components/sections/hero
</verification>

<success_criteria>
- TrustSignal shows "573 עסקים סומכים על Findo" below CTA
- StickyCtaBar appears on mobile after 400px scroll
- ActivityFeed animates inside PhoneMockup
- Homepage renders complete hero section
- Primary CTA visible without scrolling (above fold)
- All components properly exported from hero index
- Page still builds and deploys successfully
</success_criteria>

<output>
After completion, create `.planning/phases/14-hero-first-impression/14-03-SUMMARY.md`
</output>
