---
phase: 15-social-proof-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/ui/carousel.tsx
  - website/components/sections/social-proof/TestimonialCard.tsx
  - website/components/sections/social-proof/TestimonialsCarousel.tsx
autonomous: true

must_haves:
  truths:
    - "Testimonial carousel swipes correctly in RTL (right-to-left)"
    - "Each testimonial shows photo, name, business, and specific metric"
    - "Carousel has navigation arrows that work on desktop"
    - "Mobile users can swipe horizontally through testimonials"
  artifacts:
    - path: "website/components/ui/carousel.tsx"
      provides: "shadcn/ui Carousel with RTL support"
      contains: "CarouselContent"
    - path: "website/components/sections/social-proof/TestimonialCard.tsx"
      provides: "Individual testimonial card with full attribution"
      exports: ["TestimonialCard"]
    - path: "website/components/sections/social-proof/TestimonialsCarousel.tsx"
      provides: "Carousel section with multiple testimonials"
      exports: ["TestimonialsCarousel"]
  key_links:
    - from: "TestimonialsCarousel.tsx"
      to: "carousel.tsx"
      via: "Carousel, CarouselContent, CarouselItem imports"
      pattern: "from.*@/components/ui/carousel"
    - from: "TestimonialsCarousel.tsx"
      to: "TestimonialCard.tsx"
      via: "mapping testimonials to cards"
      pattern: "TestimonialCard"
---

<objective>
Create the testimonial carousel system with full attribution testimonials that satisfies PROOF-01, PROOF-02, and PROOF-03 requirements.

Purpose: Testimonials with specific metrics and variety create overwhelming social proof that builds trust.
Output: TestimonialsCarousel section ready for homepage integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-social-proof-trust/15-RESEARCH.md
@website/components/ui/card.tsx
@website/lib/animation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui Carousel and create TestimonialCard</name>
  <files>
    website/components/ui/carousel.tsx
    website/components/sections/social-proof/TestimonialCard.tsx
  </files>
  <action>
1. Install shadcn/ui Carousel component:
   ```bash
   cd website && pnpm dlx shadcn@latest add carousel
   ```
   This will install embla-carousel-react and create carousel.tsx.

2. Create `website/components/sections/social-proof/` directory.

3. Create TestimonialCard.tsx with:
   - Props: quote (string), name (string), business (string), metric (string), avatarSrc (string), industry (string)
   - Use Card component from existing ui/card.tsx
   - Layout: blockquote at top, avatar + attribution at bottom, metric badge
   - Use Next.js Image for avatar (48x48, rounded-full)
   - Use logical properties (ps-/pe-/ms-/me-) for RTL support
   - Add subtle hover effect using Motion m.div with scaleIn variant

4. Export Testimonial type for reuse:
   ```typescript
   export interface Testimonial {
     id: string;
     quote: string;
     name: string;
     business: string;
     metric: string;
     avatarSrc: string;
     industry: string;
   }
   ```
  </action>
  <verify>
    - `pnpm build` passes in website directory
    - carousel.tsx exists in website/components/ui/
    - TestimonialCard.tsx exports TestimonialCard and Testimonial type
  </verify>
  <done>
    Carousel component installed and TestimonialCard renders with full attribution (photo, name, business, metric).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TestimonialsCarousel section</name>
  <files>
    website/components/sections/social-proof/TestimonialsCarousel.tsx
    website/components/sections/social-proof/index.ts
  </files>
  <action>
1. Create TestimonialsCarousel.tsx:
   - Import Carousel components from @/components/ui/carousel
   - Import TestimonialCard and Testimonial type from ./TestimonialCard
   - Configure carousel with RTL support:
     ```typescript
     <Carousel
       dir="rtl"
       opts={{
         direction: "rtl",
         align: "start",
         loop: true,
       }}
     >
     ```
   - Use CarouselContent with `-ms-4` for RTL gap handling
   - Use CarouselItem with `ps-4 basis-full md:basis-1/2 lg:basis-1/3`
   - Add CarouselPrevious and CarouselNext with `rtl:rotate-180` class
   - Add section heading: "מה הלקוחות שלנו אומרים" (What our customers say)
   - Use ScrollReveal wrapper for entrance animation

2. Add placeholder testimonials array with variety per CONTEXT.md:
   - Testimonial 1: Restaurant owner, metric focus "הגדלנו את כמות הלידים ב-40%" (PROOF-01)
   - Testimonial 2: Clinic/salon, different industry (PROOF-02)
   - Testimonial 3: Emotional outcome "סוף סוף יש לי שקט נפשי" (PROOF-03)
   - Use placeholder avatars: `/testimonials/avatar-1.jpg` etc.

3. Create index.ts barrel export:
   ```typescript
   export { TestimonialsCarousel } from "./TestimonialsCarousel";
   export { TestimonialCard, type Testimonial } from "./TestimonialCard";
   ```
  </action>
  <verify>
    - `pnpm build` passes
    - TestimonialsCarousel renders with 3+ testimonials
    - Carousel navigation arrows visible on desktop
    - `dir="rtl"` and `direction: "rtl"` both set on carousel
  </verify>
  <done>
    Testimonials carousel displays 3 testimonials with full attribution, variety in industries and outcome types, RTL-native swipe behavior.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `cd website && pnpm build` - no errors
2. Run `cd website && pnpm dev` and visit http://localhost:3000
3. Import TestimonialsCarousel in a test page to verify rendering
4. Check carousel swipes correctly (right-to-left in RTL)
5. Check arrows rotate correctly for RTL
6. Verify mobile touch swipe works
</verification>

<success_criteria>
- shadcn/ui Carousel installed with embla-carousel-react
- TestimonialCard component shows full attribution (photo, name, business, metric)
- TestimonialsCarousel displays 3 testimonials with variety (PROOF-01, PROOF-02, PROOF-03)
- Carousel respects RTL direction (swipe and arrows)
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-social-proof-trust/15-01-SUMMARY.md`
</output>
