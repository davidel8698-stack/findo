---
phase: 15-social-proof-trust
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/social-proof/FloatingActivityWidget.tsx
  - website/components/sections/social-proof/TrustBadges.tsx
  - website/components/sections/social-proof/GuaranteeBadge.tsx
autonomous: true

must_haves:
  truths:
    - "Floating widget shows activity notifications that cycle automatically"
    - "Widget appears in bottom-start corner (RTL-aware)"
    - "Trust badges show partner/authority logos"
    - "Guarantee badge appears in two variants (inline and full)"
  artifacts:
    - path: "website/components/sections/social-proof/FloatingActivityWidget.tsx"
      provides: "Floating notification widget with activity feed"
      exports: ["FloatingActivityWidget"]
    - path: "website/components/sections/social-proof/TrustBadges.tsx"
      provides: "Authority and partner logo badges"
      exports: ["TrustBadges"]
    - path: "website/components/sections/social-proof/GuaranteeBadge.tsx"
      provides: "Money-back guarantee badge"
      exports: ["GuaranteeBadge"]
  key_links:
    - from: "FloatingActivityWidget.tsx"
      to: "motion/react AnimatePresence"
      via: "animate notifications in/out"
      pattern: "AnimatePresence"
    - from: "GuaranteeBadge.tsx"
      to: "lucide-react ShieldCheck"
      via: "guarantee icon"
      pattern: "ShieldCheck"
---

<objective>
Create floating activity widget, trust badges, and guarantee badge that satisfy PROOF-05, PROOF-08, TRUST-03, TRUST-05, and TRUST-07 requirements.

Purpose: Floating activity creates sense of live platform. Authority badges and guarantees reduce risk perception.
Output: Three reusable components for trust building across the site.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-social-proof-trust/15-RESEARCH.md
@.planning/phases/15-social-proof-trust/15-CONTEXT.md
@website/lib/animation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FloatingActivityWidget</name>
  <files>
    website/components/sections/social-proof/FloatingActivityWidget.tsx
  </files>
  <action>
1. Create Activity interface:
   ```typescript
   interface Activity {
     id: string;
     type: "signup" | "review" | "lead";
     message: string;
     location: string;
     timeAgo: string;
   }
   ```

2. Create pre-set activities array (simulated, per CONTEXT.md - no dark patterns):
   - "עסק חדש הצטרף לשירות" (New business joined) - תל אביב, לפני 2 דקות
   - "5 לידים חדשים התקבלו" (5 new leads received) - חיפה, לפני 5 דקות
   - "תגובה אוטומטית לביקורת" (Automatic review reply) - ירושלים, לפני 8 דקות
   - "פוסט חדש פורסם בגוגל" (New post published on Google) - באר שבע, לפני 12 דקות
   - More activities for variety (8-10 total)

3. Create FloatingActivityWidget component:
   - "use client" directive (timers and state)
   - useState for currentIndex and isVisible
   - useEffect for timing logic:
     - Show first notification after 5 second delay
     - Cycle interval: show for 5 seconds, hide for 3 seconds, then next
   - Use Motion AnimatePresence for enter/exit animations
   - Animation: slide up + fade in, slide up + fade out
   - Use springBouncy from lib/animation.ts
   - Position: `fixed bottom-4 start-4 z-50` (RTL-aware with start-)

4. Widget card design:
   - Small card (max-w-xs)
   - Icon based on activity type (UserPlus, Star, Users)
   - Message text (font-medium)
   - Location + timeAgo (text-xs, muted)
   - Optional close button to dismiss permanently (localStorage flag)

5. CRITICAL: Per TRUST-08, activities must be realistic and clearly simulated (no fake urgency like "hurry!").
  </action>
  <verify>
    - Component mounts and shows first notification after delay
    - AnimatePresence handles enter/exit animations
    - Position uses start-4 not left-4 (RTL-aware)
    - Activities cycle without restart on visibility
  </verify>
  <done>
    FloatingActivityWidget cycles through pre-set activities with smooth animations, positioned RTL-aware in bottom corner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TrustBadges and GuaranteeBadge</name>
  <files>
    website/components/sections/social-proof/TrustBadges.tsx
    website/components/sections/social-proof/GuaranteeBadge.tsx
  </files>
  <action>
1. Create TrustBadges.tsx:
   - Display partner/authority logos in a flex row
   - Badges to show (per CONTEXT.md):
     - Google Partner (placeholder: /badges/google-partner.svg)
     - Meta Partner (placeholder: /badges/meta-partner.svg)
     - PayPlus secured (placeholder: /badges/payplus.svg)
     - SSL Secure (can use Shield icon or placeholder)
   - Use Next.js Image with width={120} height={40}
   - Grayscale by default, full color on hover
   - Layout: centered flex-wrap with gap-6
   - Props: className (optional), size: "sm" | "md" (default md)

2. Create placeholder SVGs directory structure:
   - Create `website/public/badges/` directory
   - Note in code comments that real badge SVGs needed

3. Create GuaranteeBadge.tsx with two variants (per RESEARCH.md):
   - Props: variant ("inline" | "full"), className (optional)
   - "inline" variant:
     - Compact: ShieldCheck icon + "אחריות 100% החזר כספי" text
     - For placement near CTAs
     - Flex row, gap-1.5, text-sm
   - "full" variant:
     - Card-like: title + description
     - Title: "ההבטחה של Findo"
     - Description: "לא מרוצה? קבל החזר מלא תוך 30 יום, ללא שאלות."
     - bg-primary/5, border-primary/20, rounded-lg, p-4
   - Use ShieldCheck from lucide-react

4. Update index.ts to export all new components:
   ```typescript
   export { FloatingActivityWidget } from "./FloatingActivityWidget";
   export { TrustBadges } from "./TrustBadges";
   export { GuaranteeBadge } from "./GuaranteeBadge";
   ```
  </action>
  <verify>
    - `pnpm build` passes
    - TrustBadges renders without errors (missing images OK for now)
    - GuaranteeBadge renders both variants correctly
    - ShieldCheck icon imports from lucide-react
  </verify>
  <done>
    TrustBadges shows authority logos with hover effect. GuaranteeBadge has inline (near CTA) and full (section) variants.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `cd website && pnpm build` - no errors
2. Test FloatingActivityWidget timing and animation
3. Test TrustBadges layout on mobile and desktop
4. Test both GuaranteeBadge variants
5. Verify RTL positioning (start-4 instead of left-4)
6. Verify no dark patterns in activity messages
</verification>

<success_criteria>
- FloatingActivityWidget cycles through activities with 5s show / 3s hide timing
- Activities feel realistic (city names, plausible actions)
- Widget can be dismissed (localStorage persistence)
- TrustBadges displays placeholder for 4 authority logos
- GuaranteeBadge inline variant is compact for CTA placement
- GuaranteeBadge full variant is card-like for dedicated section
- All components use RTL-aware positioning (start/end not left/right)
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-social-proof-trust/15-03-SUMMARY.md`
</output>
