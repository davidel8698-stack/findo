---
phase: 15-social-proof-trust
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/social-proof/SocialProofCounters.tsx
  - website/components/sections/social-proof/TrustBadges.tsx
  - website/components/sections/social-proof/GuaranteeBadge.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Social proof counters include 24/7 availability metric"
    - "Trust badges (Google Partner, Meta Partner, PayPlus, SSL) all visible with icon fallbacks"
    - "Guarantee badge inline variant specifies '30 days'"
  artifacts:
    - path: "website/components/sections/social-proof/SocialProofCounters.tsx"
      provides: "24/7 availability as 4th metric"
      contains: "24/7"
    - path: "website/components/sections/social-proof/TrustBadges.tsx"
      provides: "Icon-based trust badges (no missing SVGs)"
      contains: "useIcon: true"
    - path: "website/components/sections/social-proof/GuaranteeBadge.tsx"
      provides: "30 days in inline variant"
      contains: "30 יום"
  key_links:
    - from: "SocialProofCounters.tsx"
      to: "homepage"
      via: "4th metric renders with static 24/7 display"
      pattern: "24/7"
    - from: "TrustBadges.tsx"
      to: "Lucide icons"
      via: "useIcon: true for all badges"
      pattern: "useIcon.*true"
---

<objective>
Close 3 UAT gaps in Phase 15 Social Proof components.

Purpose: Fix issues discovered during UAT testing so Phase 15 achieves 16/16 tests passing.
Output: Updated components with all trust signals displaying correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-social-proof-trust/15-UAT.md
@website/components/sections/social-proof/SocialProofCounters.tsx
@website/components/sections/social-proof/TrustBadges.tsx
@website/components/sections/social-proof/GuaranteeBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 24/7 availability metric to SocialProofCounters</name>
  <files>website/components/sections/social-proof/SocialProofCounters.tsx</files>
  <action>
Add a 4th metric for 24/7 availability. Since "24/7" is not a numeric value that can be animated, create a StaticMetric component that displays it with a fade-in animation instead of counting.

Implementation:
1. Create StaticMetric component alongside AnimatedCounter:
   - Takes `value` (string like "24/7"), `label` (Hebrew text)
   - Uses ScrollReveal for fade-in when scrolled into view
   - Same styling as AnimatedCounter (text-4xl/5xl bold primary)

2. Add 4th metric to the metrics array conceptually, but render StaticMetric for the 24/7 item:
   ```tsx
   {
     type: "static",
     value: "24/7",
     label: "זמינות מלאה",
   }
   ```

3. Update grid to 4 columns on desktop: `md:grid-cols-4`

4. Render logic:
   - If metric has `target` (number): use AnimatedCounter
   - If metric has `value` (string): use StaticMetric
  </action>
  <verify>
Run `npm run build` in website directory - no TypeScript errors.
Visually confirm 4 metrics appear: 573+ customers, 12,400+ reviews, 8,500+ leads, 24/7 availability.
  </verify>
  <done>SocialProofCounters displays 4 metrics including "24/7 זמינות מלאה" with fade-in animation.</done>
</task>

<task type="auto">
  <name>Task 2: Convert TrustBadges to icon-based display</name>
  <files>website/components/sections/social-proof/TrustBadges.tsx</files>
  <action>
Replace Image-based badges with Lucide icon fallbacks for all badges. This matches the existing SSL badge pattern and ensures no missing images.

Implementation:
1. Import required Lucide icons at top:
   ```tsx
   import { Shield, BadgeCheck, CreditCard, Award } from "lucide-react";
   ```

2. Update Badge interface:
   ```tsx
   interface Badge {
     icon: React.ComponentType<{ className?: string }>;
     text: string;
     alt: string;
   }
   ```

3. Update badges array to use icons for ALL badges:
   ```tsx
   const badges: Badge[] = [
     {
       icon: Award,
       text: "Google Partner",
       alt: "Google Partner",
     },
     {
       icon: BadgeCheck,
       text: "Meta Partner",
       alt: "Meta Partner",
     },
     {
       icon: CreditCard,
       text: "PayPlus",
       alt: "PayPlus - תשלום מאובטח",
     },
     {
       icon: Shield,
       text: "SSL Secure",
       alt: "SSL Secure - אתר מאובטח",
     },
   ];
   ```

4. Remove all Image imports and onError handling.

5. Render each badge as icon + text (similar to current SSL rendering):
   ```tsx
   {badges.map((badge) => (
     <div
       key={badge.alt}
       className="relative grayscale opacity-60 hover:grayscale-0 hover:opacity-100 transition-all duration-300"
     >
       <div className={cn("flex items-center gap-1.5 text-muted-foreground", config.text)}>
         <badge.icon className={size === "sm" ? "h-4 w-4" : "h-5 w-5"} />
         <span className="font-medium">{badge.text}</span>
       </div>
     </div>
   ))}
   ```

6. Remove the Image import from "next/image" since it's no longer used.
  </action>
  <verify>
Run `npm run build` in website directory - no TypeScript errors.
Visually confirm all 4 badges appear with icons: Google Partner (Award), Meta Partner (BadgeCheck), PayPlus (CreditCard), SSL (Shield).
  </verify>
  <done>TrustBadges shows 4 icon-based trust indicators, all visible without missing image errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add "30 days" to GuaranteeBadge inline variant</name>
  <files>website/components/sections/social-proof/GuaranteeBadge.tsx</files>
  <action>
Update the inline variant text to explicitly mention "30 days" as required by UAT test 11.

Current inline text: "אחריות 100% החזר כספי"
New inline text: "אחריות 30 יום - החזר כספי מלא"

Implementation:
1. Find the inline variant return block (line ~34):
   ```tsx
   <span>אחריות 100% החזר כספי</span>
   ```

2. Replace with:
   ```tsx
   <span>אחריות 30 יום - החזר כספי מלא</span>
   ```

This matches the full variant messaging which already mentions "30 יום" and ensures consistency.
  </action>
  <verify>
Run `npm run build` in website directory - no TypeScript errors.
Visually confirm inline guarantee badge shows "אחריות 30 יום - החזר כספי מלא".
  </verify>
  <done>GuaranteeBadge inline variant displays "30 יום" specification clearly.</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build verification:
   ```bash
   cd website && npm run build
   ```
   Expected: Build succeeds with no errors.

2. Visual verification (dev server):
   ```bash
   cd website && npm run dev
   ```
   Navigate to http://localhost:3000 and verify:
   - Social proof counters section shows 4 metrics (573+, 12,400+, 8,500+, 24/7)
   - Trust badges section shows 4 icon-based badges (no broken images)
   - Guarantee badge near hero CTA shows "30 יום"

3. UAT re-test readiness:
   - Test 6 (Counters): Should now pass with 24/7 metric
   - Test 10 (Trust Badges): Should now pass with icon fallbacks
   - Test 11 (Guarantee Badge): Should now pass with 30 days specified
</verification>

<success_criteria>
- [ ] SocialProofCounters displays 4 metrics including 24/7 availability
- [ ] TrustBadges shows all 4 badges using Lucide icons (no missing images)
- [ ] GuaranteeBadge inline variant includes "30 יום" text
- [ ] `npm run build` succeeds with no errors
- [ ] All 3 diagnosed UAT gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/15-social-proof-trust/15-06-SUMMARY.md`
</output>
