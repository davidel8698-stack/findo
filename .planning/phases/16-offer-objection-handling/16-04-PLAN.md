---
phase: 16-offer-objection-handling
plan: 04
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - website/components/sections/offer/FAQSection.tsx
  - website/components/sections/offer/index.ts
autonomous: true

must_haves:
  truths:
    - "User can click to expand FAQ questions"
    - "User can click to collapse FAQ questions"
    - "Only one question open at a time (single accordion)"
    - "Top 5 objections addressed in FAQ"
    - "WhatsApp CTA visible at bottom of FAQ"
  artifacts:
    - path: "website/components/sections/offer/FAQSection.tsx"
      provides: "FAQ section with accordion and WhatsApp CTA"
      exports: ["FAQSection"]
      min_lines: 60
  key_links:
    - from: "website/components/sections/offer/FAQSection.tsx"
      to: "website/components/ui/accordion.tsx"
      via: "import Accordion components"
      pattern: "from ['\"]@/components/ui/accordion['\"]"
---

<objective>
Create the FAQ section with accordion format addressing top objections.

Purpose: Answer common questions before they become objections. Ends with WhatsApp CTA for remaining questions.
Output: FAQSection component with collapsible accordion and "Still have questions?" WhatsApp link.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-offer-objection-handling/16-CONTEXT.md
@.planning/phases/16-offer-objection-handling/16-RESEARCH.md

@website/components/ui/accordion.tsx
@website/components/sections/trust/ContactSection.tsx
@website/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ Section with accordion</name>
  <files>website/components/sections/offer/FAQSection.tsx</files>
  <action>
Create FAQSection.tsx with FAQ content in accordion format.

**FAQ content (per CONTEXT.md - top 5 SMB objections):**
```typescript
const faqItems = [
  {
    question: "כמה זמן לוקח להתחיל?",
    answer: "הגדרה תוך 2 דקות. מחברים את חשבון Google Business שלכם ומתחילים לעבוד מיד.",
  },
  {
    question: "אפשר לבטל בכל רגע?",
    answer: "כן. ללא התחייבות, ללא דמי ביטול. מבטלים בקליק אחד.",
  },
  {
    question: "מה ההבדל מלעשות לבד?",
    answer: "Findo עובד 24/7 בלי הפסקה. עונה לביקורות, לוכד לידים, ומבקש ביקורות - הכל אוטומטי בזמן שאתם מתמקדים בעסק.",
  },
  {
    question: "מה אם זה לא עובד לעסק שלי?",
    answer: "יש לנו אחריות 14 יום. לא מרוצים? החזר מלא ללא שאלות. בנוסף, אנחנו מתחייבים ל-10 ביקורות חדשות בחודש הראשון.",
  },
  {
    question: "אני צריך עזרה, יש תמיכה?",
    answer: "כן! צוות התמיכה שלנו זמין בוואטסאפ. שאלה? שלחו הודעה ונחזור אליכם תוך דקות.",
  },
];
```

**Component structure:**
```
<section className="py-16 md:py-24">
  <container>
    <ScrollReveal>
      <heading: "שאלות נפוצות" />
    </ScrollReveal>

    <div className="max-w-2xl mx-auto">
      <Accordion type="single" collapsible>
        {faqItems.map((item, index) => (
          <AccordionItem key={index} value={`item-${index}`}>
            <AccordionTrigger>{item.question}</AccordionTrigger>
            <AccordionContent>{item.answer}</AccordionContent>
          </AccordionItem>
        ))}
      </Accordion>

      <!-- WhatsApp CTA at bottom -->
      <div className="mt-8 text-center">
        <p className="text-muted-foreground mb-4">עדיין יש לכם שאלות?</p>
        <Button variant="outline" asChild>
          <a href="https://wa.me/972XXXXXXXXX?text=שלום, יש לי שאלה על Findo">
            <MessageCircle className="me-2 h-4 w-4" />
            שלחו לנו הודעה בוואטסאפ
          </a>
        </Button>
      </div>
    </div>
  </container>
</section>
```

**Key implementation details:**
- "use client" directive (accordion uses state)
- Import Accordion components from @/components/ui/accordion
- Import ScrollReveal from @/components/motion
- Import Button from @/components/ui/button
- Use MessageCircle icon from lucide-react for WhatsApp
- WhatsApp URL: https://wa.me/972XXXXXXXXX (placeholder, will be updated)
- type="single" collapsible for one-at-a-time behavior per CONTEXT.md
- Use me-2 (not mr-2) for icon spacing (RTL support)
  </action>
  <verify>
Run `npm run build` from website directory - no TypeScript errors.
File exists at website/components/sections/offer/FAQSection.tsx.
Grep for "collapsible" to confirm accordion configuration.
  </verify>
  <done>
FAQSection.tsx created with:
- 5 FAQ items addressing common objections
- Accordion with single/collapsible behavior
- WhatsApp CTA at bottom
- ScrollReveal animation on heading
  </done>
</task>

<task type="auto">
  <name>Task 2: Update offer section barrel export</name>
  <files>website/components/sections/offer/index.ts</files>
  <action>
Add FAQSection to the barrel export.

Update index.ts:
```typescript
export { GuaranteeBadge, type GuaranteeBadgeProps, type GuaranteeType } from "./GuaranteeBadges";
export { ZeroRiskSummary } from "./ZeroRiskSummary";
export { ROICalculator } from "./ROICalculator";
export { FAQSection } from "./FAQSection";
```
  </action>
  <verify>
Run `npm run build` from website directory - no TypeScript errors.
Grep for "FAQSection" in index.ts.
  </verify>
  <done>
FAQSection exported from offer section barrel.
Import from @/components/sections/offer works.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. FAQSection.tsx exists with 5 FAQ items
3. Accordion uses type="single" collapsible
4. WhatsApp CTA present at bottom
5. index.ts exports FAQSection
</verification>

<success_criteria>
- 5 FAQ questions visible in accordion format
- Click to expand/collapse works
- Only one question open at a time
- WhatsApp CTA visible below FAQ
- CSS animation on expand/collapse (from globals.css keyframes)
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-offer-objection-handling/16-04-SUMMARY.md`
</output>
