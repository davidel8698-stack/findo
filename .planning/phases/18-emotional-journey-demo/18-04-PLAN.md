---
phase: 18-emotional-journey-demo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/motion/variants.ts
  - website/components/ui/button.tsx
  - website/components/ui/card.tsx
  - website/app/globals.css
autonomous: true

must_haves:
  truths:
    - "Button hover feels playful with scale transform"
    - "Card hover elevates with shadow increase"
    - "All scroll animations use stagger reveal pattern"
    - "Premium feel throughout (no janky animations)"
  artifacts:
    - path: "website/components/motion/variants.ts"
      provides: "Hover variants for micro-interactions"
      contains: "buttonHover"
    - path: "website/components/ui/button.tsx"
      provides: "Motion-enhanced Button component"
      contains: "whileHover"
  key_links:
    - from: "button.tsx"
      to: "motion/react"
      via: "m.button wrapper"
      pattern: "m\\.button"
    - from: "variants.ts"
      to: "lib/animation"
      via: "spring presets"
      pattern: "springBouncy"
---

<objective>
Add delightful micro-interactions throughout the website - playful button hovers, card elevations, and polished scroll animations. Every interaction should feel intentional and premium.

Purpose: Create the "feels expensive" polish that separates good websites from great ones. Micro-interactions reinforce the playful-but-professional Findo brand and make the experience memorable.

Output: Enhanced Button/Card components with Motion, new hover variants, polished animations throughout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-emotional-journey-demo/18-CONTEXT.md
@.planning/phases/18-emotional-journey-demo/18-RESEARCH.md

# Existing animation infrastructure
@website/components/motion/variants.ts
@website/lib/animation.ts
@website/components/ui/button.tsx
@website/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hover variants to animation system</name>
  <files>website/components/motion/variants.ts</files>
  <action>
Update variants.ts with new hover variants:

```typescript
/**
 * Reusable Motion variants for consistent animations
 * Uses spring physics for playful character
 */

import { Variants } from "motion/react";
import { springBouncy, springGentle, springSnappy, stagger } from "@/lib/animation";

// ... existing variants (fadeInUp, fadeInDown, etc.) ...

// ===== NEW HOVER VARIANTS =====

// Button hover - playful scale with spring
export const buttonHover: Variants = {
  initial: { scale: 1 },
  hover: {
    scale: 1.02,
    transition: springBouncy,
  },
  tap: {
    scale: 0.98,
    transition: { duration: 0.1 },
  },
};

// Card hover - elevation with shadow
export const cardHover: Variants = {
  initial: {
    y: 0,
    boxShadow: "0 1px 3px rgba(0,0,0,0.12), 0 1px 2px rgba(0,0,0,0.06)",
  },
  hover: {
    y: -4,
    boxShadow: "0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.1)",
    transition: springGentle,
  },
};

// Icon spin on hover (for decorative icons)
export const iconSpin: Variants = {
  initial: { rotate: 0 },
  hover: {
    rotate: 15,
    transition: springBouncy,
  },
};

// Link underline expand
export const linkUnderline: Variants = {
  initial: { width: 0 },
  hover: {
    width: "100%",
    transition: { duration: 0.2 },
  },
};

// Bounce in (for attention-grabbing elements)
export const bounceIn: Variants = {
  hidden: {
    opacity: 0,
    scale: 0.3,
  },
  visible: {
    opacity: 1,
    scale: 1,
    transition: {
      ...springBouncy,
      opacity: { duration: 0.2 },
    },
  },
};

// Slide in from side (RTL-aware)
export const slideInEnd: Variants = {
  hidden: {
    opacity: 0,
    x: 50, // Positive = from end in RTL
  },
  visible: {
    opacity: 1,
    x: 0,
    transition: springGentle,
  },
};

export const slideInStart: Variants = {
  hidden: {
    opacity: 0,
    x: -50, // Negative = from start in RTL
  },
  visible: {
    opacity: 1,
    x: 0,
    transition: springGentle,
  },
};
```

New variants added:
- buttonHover: scale 1.02 on hover, 0.98 on tap
- cardHover: y:-4 lift with enhanced shadow
- iconSpin: 15deg rotation for decorative icons
- linkUnderline: width animation for text links
- bounceIn: attention-grabbing entrance
- slideInEnd/Start: RTL-aware slide animations
  </action>
  <verify>
Run: `cd website && npm run build`
- No TypeScript errors
- All variants export correctly
- Spring presets imported from lib/animation
  </verify>
  <done>
New hover variants added to animation system (buttonHover, cardHover, iconSpin, bounceIn, slideIn)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AnimatedButton component with Motion</name>
  <files>website/components/ui/button.tsx</files>
  <action>
Update button.tsx to add AnimatedButton variant:

```typescript
import * as React from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { m, type HTMLMotionProps } from "motion/react";

import { cn } from "@/lib/utils";
import { springBouncy } from "@/lib/animation";

const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-lg text-base font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-5 [&_svg]:shrink-0",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/90",
        destructive:
          "bg-destructive text-destructive-foreground hover:bg-destructive/90",
        outline:
          "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
        secondary:
          "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-12 px-6 py-3 text-base", // 48px touch target
        sm: "h-12 px-4 text-sm",
        lg: "h-14 px-8 text-lg",
        icon: "h-12 w-12", // 48px square
      },
      loading: {
        true: "pointer-events-none animate-shimmer bg-gradient-to-r from-[hsl(var(--primary))] via-[hsl(var(--primary)/0.8)] to-[hsl(var(--primary))] bg-[length:200%_100%]",
        false: "",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
      loading: false,
    },
  }
);

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean;
  loading?: boolean;
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, loading = false, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : "button";
    return (
      <Comp
        className={cn(buttonVariants({ variant, size, loading, className }))}
        ref={ref}
        disabled={loading || props.disabled}
        {...props}
      />
    );
  }
);
Button.displayName = "Button";

// ===== NEW: AnimatedButton with Motion micro-interactions =====

export interface AnimatedButtonProps
  extends Omit<HTMLMotionProps<"button">, "ref">,
    VariantProps<typeof buttonVariants> {
  loading?: boolean;
}

const AnimatedButton = React.forwardRef<HTMLButtonElement, AnimatedButtonProps>(
  ({ className, variant, size, loading = false, children, ...props }, ref) => {
    return (
      <m.button
        ref={ref}
        className={cn(buttonVariants({ variant, size, loading, className }))}
        disabled={loading || props.disabled}
        whileHover={{ scale: 1.02 }}
        whileTap={{ scale: 0.98 }}
        transition={springBouncy}
        {...props}
      >
        {children}
      </m.button>
    );
  }
);
AnimatedButton.displayName = "AnimatedButton";

export { Button, AnimatedButton, buttonVariants };
```

Key changes:
- Import motion/react and spring presets
- AnimatedButton uses m.button with whileHover/whileTap
- Keep regular Button for compatibility (asChild support)
- AnimatedButton has same variants/sizes as Button
- springBouncy transition for playful feel

Note: AnimatedButton cannot support asChild due to Motion limitations, but that's fine - asChild is used for Link components which have their own hover states.
  </action>
  <verify>
Run: `cd website && npm run build`
- No TypeScript errors
- Button and AnimatedButton both export
- Motion types resolve correctly
  </verify>
  <done>
AnimatedButton component created with scale hover/tap effects using spring physics
  </done>
</task>

<task type="auto">
  <name>Task 3: Add hover effects to Card and global polish</name>
  <files>website/components/ui/card.tsx, website/app/globals.css</files>
  <action>
1. Update card.tsx to add AnimatedCard:

```typescript
import * as React from "react";
import { m, type HTMLMotionProps } from "motion/react";

import { cn } from "@/lib/utils";
import { springGentle } from "@/lib/animation";

const Card = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      "rounded-xl border bg-card text-card-foreground shadow-sm",
      className
    )}
    {...props}
  />
));
Card.displayName = "Card";

// ===== NEW: AnimatedCard with hover elevation =====

const AnimatedCard = React.forwardRef<
  HTMLDivElement,
  Omit<HTMLMotionProps<"div">, "ref">
>(({ className, ...props }, ref) => (
  <m.div
    ref={ref}
    className={cn(
      "rounded-xl border bg-card text-card-foreground shadow-sm",
      className
    )}
    whileHover={{
      y: -4,
      boxShadow: "0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.1)",
    }}
    transition={springGentle}
    {...props}
  />
));
AnimatedCard.displayName = "AnimatedCard";

// ... rest of card components (CardHeader, CardTitle, etc.) unchanged ...

export { Card, AnimatedCard, CardHeader, CardFooter, CardTitle, CardDescription, CardContent };
```

2. Add global CSS polish in globals.css:

```css
/* Add after existing styles */

/* Smooth scroll behavior (for non-Lenis browsers) */
@media (prefers-reduced-motion: no-preference) {
  html {
    scroll-behavior: smooth;
  }
}

/* Focus visible outlines with primary color */
:focus-visible {
  outline: 2px solid hsl(var(--primary));
  outline-offset: 2px;
}

/* Selection color matching brand */
::selection {
  background-color: hsl(var(--primary) / 0.2);
  color: inherit;
}

/* Smooth image loading */
img {
  transition: opacity 0.3s ease-in-out;
}

/* Interactive elements get cursor pointer */
button:not(:disabled),
[role="button"]:not(:disabled),
a[href] {
  cursor: pointer;
}

/* Reduced motion: disable animations */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

Polish additions:
- AnimatedCard with y:-4 elevation on hover
- Focus visible with primary color outline
- Selection color matching brand
- Cursor pointer on interactive elements
- Reduced motion support (WCAG)
  </action>
  <verify>
Run: `cd website && npm run build`
- No TypeScript errors
- AnimatedCard exports from card.tsx
- CSS compiles without errors
  </verify>
  <done>
AnimatedCard created, global CSS polish added for focus states, selection, reduced motion
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `cd website && npm run build`
2. Visual check at localhost:3000:
   - Primary buttons have subtle scale on hover (scale 1.02)
   - Buttons have press-down effect on click (scale 0.98)
   - Test AnimatedCard in a component if used
   - Focus states use primary color ring
   - Text selection uses brand color
3. Accessibility: Check with prefers-reduced-motion simulation
4. Performance: No animation jank on hover/scroll
</verification>

<success_criteria>
- EMOTION-05: Micro-interactions that delight (button hover, card elevation)
- EMOTION-07: Visual design that conveys premium (polished focus states, selection)
- Hover effects feel bouncy/playful matching Findo brand
- Reduced motion users see no animations
- All hover transitions under 300ms (responsive feel)
</success_criteria>

<output>
After completion, create `.planning/phases/18-emotional-journey-demo/18-04-SUMMARY.md`
</output>
