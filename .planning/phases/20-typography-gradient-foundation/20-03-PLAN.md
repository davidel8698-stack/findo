---
phase: 20-typography-gradient-foundation
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified:
  - website/components/sections/social-proof/SocialProofCounters.tsx
  - website/components/sections/offer/PricingSection.tsx
autonomous: false

must_haves:
  truths:
    - "Stats section headline displays gradient text"
    - "Pricing section headline displays gradient text"
    - "Gradient text is applied only where impactful (not overdone)"
    - "All gradient headlines render correctly in RTL with Hebrew"
  artifacts:
    - path: "website/components/sections/social-proof/SocialProofCounters.tsx"
      provides: "Stats headline with gradient"
      contains: "text-gradient-brand"
    - path: "website/components/sections/offer/PricingSection.tsx"
      provides: "Pricing headline with gradient"
      contains: "text-gradient-brand"
  key_links:
    - from: "website/components/sections/social-proof/SocialProofCounters.tsx"
      to: "website/app/globals.css"
      via: "text-gradient-brand utility"
      pattern: "text-gradient-brand"
    - from: "website/components/sections/offer/PricingSection.tsx"
      to: "website/app/globals.css"
      via: "text-gradient-brand utility"
      pattern: "text-gradient-brand"
---

<objective>
Apply gradient text to key section headlines and verify visual quality.

Purpose: Extend the premium gradient visual language to Stats and Pricing sections. These are high-impact sections where gradient adds visual interest without overdoing it. Final visual verification ensures quality.

Output:
- Stats headline with gradient
- Pricing headline with gradient
- Visual verification of all gradient text
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\דודאלמועלם\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-typography-gradient-foundation/20-CONTEXT.md
@.planning/phases/20-typography-gradient-foundation/20-01-SUMMARY.md
@.planning/phases/20-typography-gradient-foundation/20-02-SUMMARY.md
@website/components/sections/social-proof/SocialProofCounters.tsx
@website/components/sections/offer/PricingSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply gradient to Stats section headline</name>
  <files>website/components/sections/social-proof/SocialProofCounters.tsx</files>
  <action>
Update the h2 headline in SocialProofCounters.tsx to use gradient text:

Find the h2 element and update its className:
```tsx
<h2
  id="social-proof-heading"
  className={cn(
    "text-2xl md:text-3xl",
    "font-bold",
    "text-center mb-12",
    "text-gradient-brand",
    "text-shadow-[0_0_15px_rgba(249,115,22,0.35)]"
  )}
>
  המספרים מדברים בעד עצמם
</h2>
```

Key changes:
- Add `text-gradient-brand` utility
- Add subtle text-shadow for glow
- May need to import `cn` from "@/lib/utils" if not already imported
- Keep existing id for accessibility

The gradient adds visual impact to this key social proof section.
  </action>
  <verify>
Run: `npm run dev`
Check: Stats section headline "המספרים מדברים בעד עצמם" shows orange-amber gradient
Check: Gradient direction matches hero (135deg diagonal)
  </verify>
  <done>
Stats section h2 headline displays gradient text with glow, matching hero style
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply gradient to Pricing section headline</name>
  <files>website/components/sections/offer/PricingSection.tsx</files>
  <action>
Update the h2 headline in PricingSection.tsx to use gradient text:

Find the h2 element and update its className:
```tsx
<h2
  id="pricing-heading"
  className={cn(
    "text-3xl md:text-4xl",
    "font-bold",
    "text-center mb-2",
    "text-gradient-brand",
    "text-shadow-[0_0_15px_rgba(249,115,22,0.35)]"
  )}
>
  השוואה מהירה
</h2>
```

Key changes:
- Add `text-gradient-brand` utility
- Add subtle text-shadow for glow
- Import `cn` from "@/lib/utils" if not already imported
- Keep existing id for accessibility

The pricing section is a conversion-critical area where gradient draws attention.
  </action>
  <verify>
Run: `npm run dev`
Check: Pricing section headline "השוואה מהירה" shows orange-amber gradient
Check: Gradient matches hero and stats sections
  </verify>
  <done>
Pricing section h2 headline displays gradient text with glow, consistent with other sections
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete typography and gradient foundation:
1. Hero headline with orange-amber gradient + glow
2. Stats section headline with gradient
3. Pricing section headline with gradient
4. Hebrew body text with 1.8 line-height
5. Secondary text using zinc-400 color
6. Typography hierarchy through size AND color
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:3000

**Verify Hero Section:**
- Headline "יותר לקוחות. פחות עבודה." has orange-to-amber gradient
- Subtle orange glow visible behind text
- Hebrew characters render correctly (no clipping)

**Verify Stats Section (scroll down):**
- Headline "המספרים מדברים בעד עצמם" has same gradient style
- Counter labels are zinc-400 (muted gray)

**Verify Pricing Section (scroll down):**
- Headline "השוואה מהירה" has same gradient style
- Subtitle text is zinc-400

**Verify Typography Hierarchy:**
- Clear visual distinction between gradient headlines and body text
- Secondary text (zinc-400) is noticeably muted compared to primary
- Body text in hero subheadline feels readable (1.8 line-height)

**Verify RTL:**
- Gradient direction is consistent (top-left to bottom-right)
- No visual flipping or strange behavior

**Expected appearance:**
- Premium, Linear/Stripe quality gradient
- Warm orange-amber creates cohesive brand feel
- Glow is subtle, not overwhelming
- Clear typography hierarchy throughout
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase, or describe any visual issues to address</resume-signal>
</task>

</tasks>

<verification>
1. Stats section headline shows gradient matching hero
2. Pricing section headline shows gradient matching hero
3. All gradient text renders correctly with Hebrew characters
4. No performance issues (gradients are CSS-only)
5. Visual consistency across all gradient headlines
6. Typography hierarchy is clear throughout the page
</verification>

<success_criteria>
- [ ] SocialProofCounters.tsx h2 uses text-gradient-brand with glow
- [ ] PricingSection.tsx h2 uses text-gradient-brand with glow
- [ ] All gradient headlines have consistent 135deg direction
- [ ] No Hebrew character clipping or rendering issues
- [ ] User approves visual quality of gradient and typography
- [ ] `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-typography-gradient-foundation/20-03-SUMMARY.md`
</output>
