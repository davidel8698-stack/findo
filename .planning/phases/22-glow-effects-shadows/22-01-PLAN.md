---
phase: 22-glow-effects-shadows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/app/globals.css
autonomous: true

must_haves:
  truths:
    - "CSS variables for 4-layer shadows are defined in @theme block"
    - "CTA pulse animation keyframes exist and respect reduced motion"
    - "Glow variables for CTA and neutral elements are available"
  artifacts:
    - path: "website/app/globals.css"
      provides: "Shadow system variables, glow variables, pulse keyframes"
      contains: "--shadow-elevation-medium"
  key_links:
    - from: "website/app/globals.css"
      to: "@theme block"
      via: "CSS custom properties"
      pattern: "--shadow-cta"
---

<objective>
Define the CSS foundation for glow effects and multi-layer shadows.

Purpose: Establish CSS variables and keyframes that components will consume. This creates the visual vocabulary for Phase 22's glow and shadow system.

Output: Enhanced globals.css with shadow elevation variables, glow variables, and CTA pulse animation.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-glow-effects-shadows/22-CONTEXT.md
@.planning/phases/22-glow-effects-shadows/22-RESEARCH.md
@website/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadow and glow CSS variables to @theme block</name>
  <files>website/app/globals.css</files>
  <action>
Add the following CSS variables inside the existing `@theme` block, after the Background Depth System section:

```css
/* ============================================
 * GLOW & SHADOW SYSTEM - Phase 22
 * Multi-layer shadows + glow effects
 * ============================================ */

/* 4-layer shadow - medium elevation (cards) */
--shadow-elevation-medium:
  0 1px 1px hsl(0 0% 0% / 0.075),
  0 2px 2px hsl(0 0% 0% / 0.075),
  0 4px 4px hsl(0 0% 0% / 0.075),
  0 8px 8px hsl(0 0% 0% / 0.075);

/* 4-layer shadow - high elevation (CTAs, modals) */
--shadow-elevation-high:
  0 2px 2px hsl(0 0% 0% / 0.05),
  0 4px 4px hsl(0 0% 0% / 0.05),
  0 8px 8px hsl(0 0% 0% / 0.05),
  0 16px 16px hsl(0 0% 0% / 0.05);

/* CTA shadow with orange tint (brand reinforcement) */
--shadow-cta:
  0 2px 4px hsl(24.6 95% 53.1% / 0.15),
  0 4px 8px hsl(24.6 95% 53.1% / 0.1),
  0 8px 16px hsl(0 0% 0% / 0.05),
  0 16px 32px hsl(0 0% 0% / 0.05);

/* Hover shadow - deeper expansion */
--shadow-hover:
  0 4px 6px hsl(0 0% 0% / 0.07),
  0 8px 12px hsl(0 0% 0% / 0.07),
  0 16px 24px hsl(0 0% 0% / 0.07),
  0 32px 48px hsl(0 0% 0% / 0.07);

/* Glow effect - orange for CTA */
--glow-cta: 0 0 20px 8px hsl(24.6 95% 53.1% / 0.2);
--glow-cta-intense: 0 0 25px 10px hsl(24.6 95% 53.1% / 0.3);

/* Glow effect - neutral for secondary elements */
--glow-neutral: 0 0 15px 5px hsl(0 0% 100% / 0.1);
```

Place this section AFTER the `--bg-noise-size` variable and BEFORE the Z-INDEX SCALE section.
  </action>
  <verify>Grep for `--shadow-elevation-medium` in globals.css returns a match</verify>
  <done>All 8 shadow/glow CSS variables are defined in the @theme block</done>
</task>

<task type="auto">
  <name>Task 2: Add CTA pulse keyframes and utility classes</name>
  <files>website/app/globals.css</files>
  <action>
Add the following after the existing `.text-secondary` class (end of file):

```css
/* ============================================
 * CTA PULSE ANIMATION - Phase 22
 * Draws attention to primary CTA
 * 2-second cycle, pauses on hover
 * ============================================ */
@keyframes cta-pulse {
  0%, 100% {
    box-shadow:
      0 0 0 0 hsl(24.6 95% 53.1% / 0.4),
      var(--shadow-cta);
  }
  50% {
    box-shadow:
      0 0 20px 8px hsl(24.6 95% 53.1% / 0.2),
      var(--shadow-cta);
  }
}

.cta-pulse {
  animation: cta-pulse 2s ease-in-out infinite;
}

.cta-pulse:hover {
  animation-play-state: paused;
  box-shadow:
    0 0 25px 10px hsl(24.6 95% 53.1% / 0.3),
    var(--shadow-cta);
}

/* Respect reduced motion for pulse */
@media (prefers-reduced-motion: reduce) {
  .cta-pulse {
    animation: none;
    box-shadow: var(--shadow-cta);
  }
}

/* Static CTA glow (for mobile sticky bar) */
.cta-glow-static {
  box-shadow:
    var(--glow-cta),
    var(--shadow-cta);
}
```

This provides:
- `.cta-pulse` class for hero primary CTA (animated glow)
- `.cta-glow-static` class for mobile sticky bar (no animation)
- Proper reduced-motion support
  </action>
  <verify>Run `npm run build` in website directory - no CSS compilation errors</verify>
  <done>CTA pulse animation with hover pause and reduced motion support is defined</done>
</task>

</tasks>

<verification>
1. `grep --shadow-elevation-medium website/app/globals.css` returns match
2. `grep cta-pulse website/app/globals.css` returns match
3. `grep prefers-reduced-motion website/app/globals.css` shows pulse animation respects setting
4. `npm run build` in website directory completes without CSS errors
</verification>

<success_criteria>
- 4-layer shadow system variables defined (--shadow-elevation-medium, --shadow-elevation-high, --shadow-cta, --shadow-hover)
- Glow variables defined (--glow-cta, --glow-cta-intense, --glow-neutral)
- CTA pulse keyframes defined with 2-second cycle
- Pulse pauses on hover per CONTEXT.md decision
- prefers-reduced-motion respected (disables pulse animation)
- Static glow class available for mobile sticky CTA
</success_criteria>

<output>
After completion, create `.planning/phases/22-glow-effects-shadows/22-01-SUMMARY.md`
</output>
