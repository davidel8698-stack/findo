---
phase: 24-micro-interactions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/demo/DemoSection.tsx
  - website/components/sections/demo/LottieDemo.tsx
  - website/components/sections/conversion/LeadCaptureForm.tsx
  # CTAGroup and FAQSection EXCLUDED - all buttons use asChild pattern (incompatible with Motion)
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tab buttons and replay button have shadow-lift on hover (y:-1px + increased shadow)"
    - "Submit button scales 0.98 on press with reduced shadow"
    - "All micro-interactions feel snappy (150ms)"
  artifacts:
    - path: "website/components/sections/demo/DemoSection.tsx"
      provides: "AnimatedButton for tab switcher buttons"
      contains: "AnimatedButton"
    - path: "website/components/sections/demo/LottieDemo.tsx"
      provides: "AnimatedButton for replay button"
      contains: "AnimatedButton"
    - path: "website/components/sections/conversion/LeadCaptureForm.tsx"
      provides: "AnimatedButton for submit button"
      contains: "AnimatedButton"
  key_links:
    - from: "website/components/sections/demo/DemoSection.tsx"
      to: "website/components/ui/button.tsx"
      via: "import AnimatedButton"
      pattern: "import.*AnimatedButton.*from"
    - from: "website/components/sections/conversion/LeadCaptureForm.tsx"
      to: "website/components/ui/button.tsx"
      via: "import AnimatedButton"
      pattern: "import.*AnimatedButton.*from"
---

<objective>
Wire AnimatedButton into onClick-based buttons (NO asChild pattern).

Purpose: AnimatedButton uses Motion's m.button which is incompatible with Radix Slot (asChild).
This plan targets ONLY buttons with onClick handlers - tab buttons, replay button, and form submit.
Buttons using asChild (Link wrappers) retain Button with existing CSS hover effects.

Output: onClick buttons have shadow-lift on hover and scale-down on press.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-micro-interactions/24-VERIFICATION.md
@website/components/ui/button.tsx
@website/components/sections/demo/DemoSection.tsx
@website/components/sections/demo/LottieDemo.tsx
@website/components/sections/conversion/LeadCaptureForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AnimatedButton to DemoSection tab buttons</name>
  <files>website/components/sections/demo/DemoSection.tsx</files>
  <action>
    Replace Button with AnimatedButton for tab switcher buttons (lines 54-70):

    1. Update import (line 7):
       - Change `import { Button } from "@/components/ui/button";`
       - To `import { Button, AnimatedButton } from "@/components/ui/button";`

    2. Replace tab buttons (lines 54-70):
       The two Button elements with onClick handlers are perfect for AnimatedButton:

       Line 54-61: Video tab button
       - Current: `<Button variant={...} onClick={...}>`
       - Change to: `<AnimatedButton variant={...} onClick={...}>`

       Line 62-70: Interactive tab button
       - Current: `<Button variant={...} onClick={...}>`
       - Change to: `<AnimatedButton variant={...} onClick={...}>`

    3. KEEP Button for CTA (lines 94-96):
       - This uses `asChild` wrapping an anchor - keep as Button
       - Motion (m.button) is incompatible with Radix Slot pattern
       - CSS hover effects already apply here
  </action>
  <verify>
    - `grep "AnimatedButton" website/components/sections/demo/DemoSection.tsx` shows 2 usages (tab buttons)
    - `grep "Button.*asChild" website/components/sections/demo/DemoSection.tsx` shows 1 usage (CTA)
    - `npx tsc --noEmit` passes
    - Dev server: Hover tab buttons shows shadow-lift effect
  </verify>
  <done>DemoSection tab buttons use AnimatedButton with shadow-lift hover</done>
</task>

<task type="auto">
  <name>Task 2: Wire AnimatedButton to LottieDemo replay and LeadCaptureForm submit</name>
  <files>
    website/components/sections/demo/LottieDemo.tsx
    website/components/sections/conversion/LeadCaptureForm.tsx
  </files>
  <action>
    **LottieDemo.tsx - Replay button (lines 129-137):**

    1. Update import (line 7):
       - Change `import { Button } from "@/components/ui/button";`
       - To `import { Button, AnimatedButton } from "@/components/ui/button";`

    2. Replace replay button (lines 129-137):
       - This button has onClick={handleReplay}, NO asChild
       - Current: `<Button onClick={handleReplay} variant="outline" size="lg" ...>`
       - Change to: `<AnimatedButton onClick={handleReplay} variant="outline" size="lg" ...>`

    3. KEEP Button for CTA (lines 138-140):
       - Uses `asChild` wrapping anchor - keep as Button

    **LeadCaptureForm.tsx - Submit button (lines 87-93):**

    1. Update import (line 5):
       - Change `import { Button } from "@/components/ui/button";`
       - To `import { Button, AnimatedButton } from "@/components/ui/button";`

    2. Replace submit button (lines 87-93):
       - This is a form submit button with NO asChild - perfect for AnimatedButton
       - Current: `<Button type="submit" loading={isPending} className="w-full">`
       - Change to: `<AnimatedButton type="submit" loading={isPending} className="w-full">`
  </action>
  <verify>
    - `grep "AnimatedButton" website/components/sections/demo/LottieDemo.tsx` shows 1 usage
    - `grep "AnimatedButton" website/components/sections/conversion/LeadCaptureForm.tsx` shows 1 usage
    - `npx tsc --noEmit` passes
    - Dev server: Hover replay and submit buttons shows shadow-lift effect
  </verify>
  <done>Replay button and submit button use AnimatedButton with shadow-lift hover</done>
</task>

</tasks>

<verification>
1. **TypeScript**: `npx tsc --noEmit` passes
2. **Visual**: Run dev server, hover buttons in:
   - Demo section tab buttons (video/interactive switcher)
   - LottieDemo replay button (after animation completes)
   - Lead capture form submit button
3. **Effect**: AnimatedButton elements show:
   - Shadow-lift on hover (y:-1px + shadow increase)
   - Scale 0.98 on press
   - 150ms snappy transition
4. **Exclusions verified**: CTAGroup and FAQSection still use Button (asChild pattern)
</verification>

<success_criteria>
- AnimatedButton used for: DemoSection tabs (2), LottieDemo replay (1), LeadCaptureForm submit (1)
- Button retained for: DemoSection CTA, LottieDemo CTA (asChild pattern)
- TypeScript compilation succeeds
- Shadow-lift hover visible on onClick-based buttons
</success_criteria>

<output>
After completion, create `.planning/phases/24-micro-interactions/24-04-SUMMARY.md`
</output>
