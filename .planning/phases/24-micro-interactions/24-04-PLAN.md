---
phase: 24-micro-interactions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/molecules/CTAGroup.tsx
  - website/components/sections/demo/DemoSection.tsx
  - website/components/sections/demo/LottieDemo.tsx
  - website/components/sections/offer/FAQSection.tsx
  - website/components/sections/conversion/LeadCaptureForm.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Buttons have shadow-lift on hover (y:-1px + increased shadow)"
    - "Buttons scale 0.98 on press with reduced shadow"
    - "Hero CTA has scale:1.02 + glow intensification on hover"
    - "All micro-interactions feel snappy (150ms)"
  artifacts:
    - path: "website/components/molecules/CTAGroup.tsx"
      provides: "AnimatedButton integration for primary/secondary CTAs"
      contains: "AnimatedButton"
    - path: "website/components/sections/demo/DemoSection.tsx"
      provides: "AnimatedButton for demo section CTA"
      contains: "AnimatedButton"
  key_links:
    - from: "website/components/molecules/CTAGroup.tsx"
      to: "website/components/ui/button.tsx"
      via: "import AnimatedButton"
      pattern: "import.*AnimatedButton.*from"
---

<objective>
Wire AnimatedButton into all button usages across the application.

Purpose: Phase 24 components were built but never integrated. This plan replaces static Button with AnimatedButton to deliver user-facing hover/press effects.

Output: All buttons respond with shadow-lift on hover and scale-down on press.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-micro-interactions/24-VERIFICATION.md
@website/components/ui/button.tsx
@website/components/molecules/CTAGroup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AnimatedButton into CTAGroup</name>
  <files>website/components/molecules/CTAGroup.tsx</files>
  <action>
    Replace Button with AnimatedButton in CTAGroup component:

    1. Update import:
       - Change `import { Button } from "@/components/ui/button";`
       - To `import { Button, AnimatedButton } from "@/components/ui/button";`
       - Keep Button for asChild cases (Link wrapper), add AnimatedButton for direct buttons

    2. For buttons WITH asChild (wrapping Link):
       - Keep using Button (not AnimatedButton) because Motion components are incompatible with Radix Slot
       - asChild works with Button but NOT with AnimatedButton (m.button)
       - Apply hover/tap animations via CSS classes instead (the existing hover styles)

    3. For buttons WITHOUT asChild (onClick handlers, no Link wrapper):
       - Replace Button with AnimatedButton
       - This gets the shadow-lift Motion animations
       - Remove asChild prop entirely for these cases

    4. Specifically in CTAGroup:
       - If primary CTA uses asChild with Link: keep Button, rely on CSS hover
       - If primary CTA uses onClick navigation: use AnimatedButton, remove asChild
       - Same logic for secondary CTA

    Important: The key insight is asChild (Radix Slot) and Motion components are incompatible.
    Either use Button+asChild for Link wrapping, or AnimatedButton without asChild for onClick.
  </action>
  <verify>
    - `grep "AnimatedButton" website/components/molecules/CTAGroup.tsx` shows imports and usage
    - `npx tsc --noEmit` passes
    - Dev server: Hover hero CTAs shows shadow-lift effect
  </verify>
  <done>CTAGroup primary and secondary buttons use AnimatedButton with shadow-lift hover</done>
</task>

<task type="auto">
  <name>Task 2: Wire AnimatedButton to standalone buttons</name>
  <files>
    website/components/sections/demo/DemoSection.tsx
    website/components/sections/demo/LottieDemo.tsx
    website/components/sections/offer/FAQSection.tsx
    website/components/sections/conversion/LeadCaptureForm.tsx
  </files>
  <action>
    Replace Button with AnimatedButton in remaining button usages:

    **DemoSection.tsx (line 94)**:
    - If using asChild with Link: keep Button (Motion incompatible with Slot)
    - Hover effects come from existing CSS classes

    **LottieDemo.tsx (line 138)**:
    - Same logic as DemoSection for asChild buttons
    - For replay button (line 130, onClick handler): use AnimatedButton without asChild

    **FAQSection.tsx (line 61)**:
    - If using asChild with Link: keep Button
    - Hover effects via existing variant styling

    **LeadCaptureForm.tsx (line 87)**:
    - This is a submit button with NO asChild - perfect for AnimatedButton
    1. Add AnimatedButton to import
    2. Replace `<Button type="submit" loading={isPending}>` with `<AnimatedButton type="submit" loading={isPending}>`

    Key principle: asChild (Radix Slot) is incompatible with Motion components (m.button).
    - Buttons with asChild (Link wrappers) -> keep Button
    - Buttons with onClick (no Link) -> use AnimatedButton
  </action>
  <verify>
    - `grep -r "AnimatedButton" website/components/sections/` shows all usages
    - `npx tsc --noEmit` passes
    - Dev server: All buttons across demo, FAQ, form sections have hover effects
  </verify>
  <done>All standalone buttons use AnimatedButton with shadow-lift hover</done>
</task>

</tasks>

<verification>
1. **TypeScript**: `npx tsc --noEmit` passes
2. **Visual**: Run dev server, hover buttons in:
   - Hero section (primary + secondary CTAs)
   - Demo section CTA
   - FAQ section WhatsApp button
   - Lead capture form submit button
3. **Effect**: All buttons show:
   - Shadow-lift on hover (y:-1px + shadow increase)
   - Scale 0.98 on press
   - 150ms snappy transition
</verification>

<success_criteria>
- All Button imports replaced with AnimatedButton in affected files
- TypeScript compilation succeeds
- Shadow-lift hover visible on all CTA buttons
- Press effect (scale 0.98) visible when clicking
</success_criteria>

<output>
After completion, create `.planning/phases/24-micro-interactions/24-04-SUMMARY.md`
</output>
