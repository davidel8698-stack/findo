---
phase: 28-design-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website/app/globals.css
autonomous: true

must_haves:
  truths:
    - "4px-based type scale (12, 14, 16, 18, 20, 24, 32, 48, 62px) available as CSS custom properties"
    - "Typography weights match use cases (400 body, 500 caption, 600 labels, 700 headings, 800 display)"
    - "Semantic text tokens (display, heading-1/2/3, body, caption, label) include line-height and weight"
    - "Letter-spacing defined for uppercase labels (tracking 0.05em)"
    - "Responsive typography pattern documented (62px -> 48px -> 36px for H1)"
  artifacts:
    - path: "website/app/globals.css"
      provides: "Typography token system in @theme block"
      contains: "--text-display: 3.875rem"
    - path: "website/app/globals.css"
      provides: "Font weight tokens"
      contains: "--font-weight-extrabold: 800"
    - path: "website/app/globals.css"
      provides: "Line height tokens"
      contains: "--leading-tight: 1.1"
  key_links:
    - from: "website/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme directive"
      pattern: "--text-display--line-height"
    - from: "Typography tokens"
      to: "Heebo font"
      via: "font-weight values"
      pattern: "--font-weight-"
---

<objective>
Implement the complete typography token system with 4px-based scale and semantic text tokens.

Purpose: Establish consistent typography hierarchy that matches Linear's design language. This includes primitive font sizes on a 4px grid, semantic text tokens with bundled line-height/weight/tracking, and standalone utility tokens for fine-grained control.

Output: Extended globals.css @theme block with typography primitives, semantic text tokens with associated properties, font-weight tokens, line-height tokens, and letter-spacing tokens.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\דודאלמועלם\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-design-foundation/28-CONTEXT.md
@.planning/phases/28-design-foundation/28-RESEARCH.md
@website/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update primitive font size tokens to 4px grid</name>
  <files>website/app/globals.css</files>
  <action>
The existing font-size tokens use a different scale. Update them to match the 4px-based scale from requirements.

Find the existing TYPOGRAPHY SCALE section and UPDATE the values (do not add duplicates):

```css
/* ============================================
 * TYPOGRAPHY SCALE - 4px-based grid
 * Phase 28: Design Foundation (Linear spec)
 * WCAG compliant - 16px+ body text
 * ============================================ */
--font-size-xs: 0.75rem;      /* 12px - captions, labels */
--font-size-sm: 0.875rem;     /* 14px - small body */
--font-size-base: 1rem;       /* 16px - body default */
--font-size-md: 1.125rem;     /* 18px - body large */
--font-size-lg: 1.25rem;      /* 20px - lead text */
--font-size-xl: 1.5rem;       /* 24px - h4 */
--font-size-2xl: 2rem;        /* 32px - h3 */
--font-size-3xl: 3rem;        /* 48px - h2 */
--font-size-4xl: 3.875rem;    /* 62px - h1 display */
```

Note: Removed 5xl/6xl/7xl (30px, 60px, 72px) - not on 4px grid. The scale now aligns: 12, 14, 16, 18, 20, 24, 32, 48, 62px.
  </action>
  <verify>
Run `npm run build` in website directory - should complete without errors.
Verify font-size-4xl is 3.875rem (62px) in browser DevTools.
  </verify>
  <done>
Font size tokens updated to 4px-based grid per requirements TYPO-01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add semantic typography tokens with associated properties</name>
  <files>website/app/globals.css</files>
  <action>
Add a new section in @theme block after the font-size tokens for semantic typography. These tokens bundle size, line-height, weight, and letter-spacing using Tailwind v4's associated properties syntax:

```css
/* ============================================
 * SEMANTIC TYPOGRAPHY TOKENS - Linear Design System
 * Phase 28: Design Foundation
 *
 * Usage: text-display applies size + line-height + weight + tracking
 * Note: 62px -> 48px -> 36px responsive handled via utility classes
 * ============================================ */

/* Display - 62px (hero headlines) */
--text-display: 3.875rem;
--text-display--line-height: 1.1;
--text-display--font-weight: 800;
--text-display--letter-spacing: -0.02em;

/* Heading 1 - 48px */
--text-heading-1: 3rem;
--text-heading-1--line-height: 1.16;
--text-heading-1--font-weight: 700;
--text-heading-1--letter-spacing: -0.01em;

/* Heading 2 - 32px */
--text-heading-2: 2rem;
--text-heading-2--line-height: 1.2;
--text-heading-2--font-weight: 700;

/* Heading 3 - 24px */
--text-heading-3: 1.5rem;
--text-heading-3--line-height: 1.3;
--text-heading-3--font-weight: 700;

/* Body large - 18px */
--text-body-large: 1.125rem;
--text-body-large--line-height: 1.6;
--text-body-large--font-weight: 400;

/* Body - 16px (default) */
--text-body: 1rem;
--text-body--line-height: 1.5;
--text-body--font-weight: 400;

/* Body small - 14px */
--text-body-small: 0.875rem;
--text-body-small--line-height: 1.5;
--text-body-small--font-weight: 400;

/* Caption - 12px */
--text-caption: 0.75rem;
--text-caption--line-height: 1.4;
--text-caption--font-weight: 500;

/* Label - 12px uppercase (TYPO-03) */
--text-label: 0.75rem;
--text-label--line-height: 1.2;
--text-label--font-weight: 600;
--text-label--letter-spacing: 0.05em;
```

These generate utility classes like `text-display`, `text-heading-1`, etc. that apply all associated properties.
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Create test element with class `text-display` - should show 62px, line-height 1.1, weight 800, letter-spacing -0.02em.
  </verify>
  <done>
Semantic typography tokens created with bundled line-height, weight, and letter-spacing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add font-weight, line-height, and letter-spacing tokens</name>
  <files>website/app/globals.css</files>
  <action>
Update the existing line-height and letter-spacing sections with Linear-aligned values, and add font-weight tokens for standalone use.

UPDATE the existing line-height section:

```css
/* Line heights - Linear Design System (TYPO-04) */
--line-height-none: 1;
--line-height-tight: 1.1;       /* Display headlines */
--line-height-snug: 1.2;        /* Headings */
--line-height-normal: 1.5;      /* Body text */
--line-height-relaxed: 1.6;     /* Hebrew/body large */
--line-height-loose: 1.8;       /* Extended prose */
```

UPDATE the existing letter-spacing section:

```css
/* Letter spacing - Linear Design System (TYPO-03) */
--letter-spacing-tighter: -0.02em;  /* Display headings */
--letter-spacing-tight: -0.01em;    /* Headings */
--letter-spacing-normal: 0;         /* Body */
--letter-spacing-wide: 0.05em;      /* Labels, uppercase */
--letter-spacing-wider: 0.1em;      /* All caps emphasis */
```

ADD a new section for font-weight tokens (after letter-spacing):

```css
/* ============================================
 * FONT WEIGHT TOKENS - Linear Design System
 * Phase 28: Design Foundation (TYPO-02)
 * Maps to Heebo weights: 400, 500, 600, 700, 800
 * ============================================ */
--font-weight-normal: 400;     /* Body text */
--font-weight-medium: 500;     /* Captions */
--font-weight-semibold: 600;   /* Labels, buttons */
--font-weight-bold: 700;       /* H2, H3 headings */
--font-weight-extrabold: 800;  /* Display headlines */
```

ADD a documentation comment about responsive typography:

```css
/* ============================================
 * RESPONSIVE TYPOGRAPHY NOTES (TYPO-05)
 *
 * For responsive H1 scaling (62px -> 48px -> 36px):
 * Use utility composition, NOT media queries in @theme
 *
 * Example:
 *   <h1 class="text-4xl md:text-heading-1 lg:text-display">
 *
 * Or define responsive variants in components:
 *   Mobile: 36px (text-4xl)
 *   Tablet: 48px (text-heading-1)
 *   Desktop: 62px (text-display)
 * ============================================ */
```
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Verify --font-weight-extrabold: 800 exists in DevTools.
Verify --line-height-tight: 1.1 exists in DevTools.
  </verify>
  <done>
Font-weight, line-height, and letter-spacing tokens defined and documented.
  </done>
</task>

</tasks>

<verification>
## Overall Plan Verification

1. **Build succeeds**: `npm run build` completes without errors in website directory
2. **Primitive tokens**: All 9 font-size values on 4px grid (12, 14, 16, 18, 20, 24, 32, 48, 62px)
3. **Semantic tokens**: text-display, text-heading-1/2/3, text-body/large/small, text-caption, text-label all exist
4. **Associated properties**: text-display class applies size + line-height + weight + tracking together
5. **Weight tokens**: All 5 weights (400, 500, 600, 700, 800) defined and match Heebo
6. **No regressions**: Existing typography renders correctly
</verification>

<success_criteria>
- [ ] Font-size tokens follow 4px grid: 12, 14, 16, 18, 20, 24, 32, 48, 62px
- [ ] Semantic text tokens include bundled line-height and font-weight
- [ ] text-label includes letter-spacing: 0.05em for uppercase
- [ ] Font-weight tokens map to Heebo weights (400, 500, 600, 700, 800)
- [ ] Line-height tokens match Linear spec (1.1 display, 1.2 headings, 1.5 body)
- [ ] Responsive typography pattern documented
- [ ] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/28-design-foundation/28-02-SUMMARY.md`
</output>
