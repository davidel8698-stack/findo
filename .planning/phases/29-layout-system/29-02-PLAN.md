---
phase: 29-layout-system
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - website/app/page.tsx
  - website/components/sections/hero/index.tsx
  - website/components/sections/emotional/PainPointSection.tsx
  - website/components/sections/emotional/ReliefSection.tsx
  - website/components/sections/demo/DemoSection.tsx
  - website/components/sections/offer/ROICalculator.tsx
  - website/components/sections/offer/PricingSection.tsx
  - website/components/sections/offer/FAQSection.tsx
autonomous: true

must_haves:
  truths:
    - "All sections use semantic padding tokens (py-section-hero, py-section-feature, py-section-cta)"
    - "No arbitrary spacing values that don't align to 4px grid"
    - "Section vertical rhythm is consistent across the page"
    - "Visual alignment looks correct at both mobile and desktop breakpoints"
  artifacts:
    - path: "website/app/page.tsx"
      provides: "Updated section wrappers with semantic padding"
      contains: "py-section"
  key_links:
    - from: "page.tsx sections"
      to: "semantic padding utilities"
      via: "className usage"
      pattern: "py-section-(hero|feature|cta)"
---

<objective>
Audit and update all page sections to use the new semantic spacing tokens, ensuring consistent 4px-grid alignment across the entire website.

Purpose: Apply the spacing system established in Plan 01 to achieve consistent visual rhythm throughout the sales page.
Output: All sections using semantic py-section-* utilities, all spacing values aligned to 4px grid.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-layout-system/29-CONTEXT.md
@.planning/phases/29-layout-system/29-01-SUMMARY.md (after Plan 01 completes)
@website/app/page.tsx
@website/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update page.tsx section wrappers with semantic padding</name>
  <files>website/app/page.tsx</files>
  <action>
Audit each section in page.tsx and replace spacing classes with semantic tokens:

**Current patterns to update:**

1. Hero Form section (line ~40): `py-8 -mt-16` - Keep as-is (special positioning)

2. Guarantee Badge section (line ~52): `py-8` - Change to `py-section-cta` (smaller spacing)

3. Social Proof Counters (line ~59): `py-16 md:py-24` -> `py-section-feature`

4. CTA After Social Proof (line ~69): `py-12` -> `py-section-cta`

5. Testimonials Carousel (line ~79): `py-16 md:py-24` -> `py-section-feature`

6. Video Testimonial (line ~97): `py-16 md:py-24` -> `py-section-feature`

7. CTA After Pricing (line ~130): `py-16` -> `py-section-cta`

8. Zero Risk Summary (line ~141): `py-16 md:py-24` -> `py-section-feature`

9. Trust Badges (line ~153): `py-16 md:py-24` -> `py-section-feature`

10. CTA After FAQ (line ~176): `py-16` -> `py-section-cta`

11. Team Section (line ~187): `py-16 md:py-24` -> `py-section-feature`

12. Contact Section (line ~198): `py-16 md:py-24` -> `py-section-feature`

13. Footer Guarantee Badge (line ~209): `py-12 pb-20 md:pb-12` -> `py-section-footer pb-20 md:pb-12` (keep pb override for sticky bar clearance)

**Classification logic:**
- Feature/content sections: Use `py-section-feature` (80px/48px)
- CTA/conversion sections: Use `py-section-cta` (64px/40px)
- Footer: Use `py-section-footer` (48px)
- Special positioning: Keep custom classes where needed

Example transformation:
```tsx
// BEFORE
<section className={cn("py-16 md:py-24", "bg-muted/30")}>

// AFTER
<section className={cn("py-section-feature", "bg-muted/30")}>
```
  </action>
  <verify>
Run `npm run dev` and visually inspect:
1. Section spacing is consistent across page
2. Mobile view (375px) shows proper mobile spacing
3. Desktop view (1440px) shows proper desktop spacing
4. No cramped or overly spaced sections
  </verify>
  <done>
All page.tsx sections updated to use semantic spacing tokens. Visual rhythm consistent across mobile and desktop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and update component section spacing</name>
  <files>
website/components/sections/hero/index.tsx
website/components/sections/emotional/PainPointSection.tsx
website/components/sections/emotional/ReliefSection.tsx
website/components/sections/demo/DemoSection.tsx
website/components/sections/offer/ROICalculator.tsx
website/components/sections/offer/PricingSection.tsx
website/components/sections/offer/FAQSection.tsx
  </files>
  <action>
Check each component file that renders its own section wrapper. Update internal spacing to align with 4px grid:

**For each component, audit:**
1. Internal section padding (if component has own `<section>` tag)
2. Internal gap/spacing values
3. Margin values

**Specific audits:**

1. **Hero (index.tsx)**: Hero likely uses `py-section-hero` or needs it. Check for `py-20` or similar and update to `py-section-hero` if the component renders its own section wrapper.

2. **PainPointSection**: If internal section wrapper exists, ensure spacing aligns. Internal `space-y-*` and `gap-*` should use 4px-grid values (4, 8, 12, 16, 24, 32, 48, 64).

3. **ReliefSection**: Same audit - check internal section padding.

4. **DemoSection**: Check if component has internal section wrapper. className prop allows external override.

5. **ROICalculator**: Likely renders own section wrapper. Update padding if needed.

6. **PricingSection**: className prop allows external override. Check internal spacing.

7. **FAQSection**: Check internal section padding and item spacing.

**Snap rules (from CONTEXT.md):**
- Non-conforming values snap to nearest 4px grid token
- 23px -> 24px (py-6)
- 47px -> 48px (py-12)
- 63px -> 64px (py-16)
- Silent exceptions for values that look correct visually

**Important:** Only modify spacing values. Do NOT change layout structure, styling, or functionality.
  </action>
  <verify>
For each modified component:
1. Run `npm run build` to verify no errors
2. Visually inspect component on page for correct spacing
3. Check mobile and desktop views
4. Confirm no CLS (cumulative layout shift) issues
  </verify>
  <done>
All component sections audited and updated. Internal spacing values align to 4px grid. No arbitrary non-conforming values remain.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. All sections in page.tsx use semantic py-section-* utilities where appropriate
3. Visual inspection shows consistent vertical rhythm
4. Mobile (375px) and desktop (1440px) both render correctly
5. No arbitrary spacing values outside 4px grid (grep for `py-\[\d+px\]` patterns)
6. Lighthouse audit shows no CLS regression
</verification>

<success_criteria>
- [ ] SPACE-06: All sections audited and updated to 4px grid alignment
- [ ] Hero section: Uses py-section-hero (120px/72px)
- [ ] Feature sections: Use py-section-feature (80px/48px)
- [ ] CTA sections: Use py-section-cta (64px/40px)
- [ ] Footer: Uses py-section-footer (48px)
- [ ] No `py-[arbitrary]` values that don't fit 4px grid
- [ ] Visual rhythm is consistent from Hero to Footer
</success_criteria>

<output>
After completion, create `.planning/phases/29-layout-system/29-02-SUMMARY.md`
</output>
