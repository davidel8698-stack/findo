---
phase: 30-component-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/ui/button.tsx
  - website/lib/animation.ts
autonomous: true

must_haves:
  truths:
    - "Primary button has gradient background with -2px hover lift"
    - "Primary button scales to 0.95 on active/tap with bouncy spring"
    - "Secondary button has transparent bg with 20% white border, 5% bg on hover"
    - "Ghost button has no bg, muted text, white text on hover"
    - "Button sizes match spec: S(32px), M(40px), L(48px)"
  artifacts:
    - path: "website/components/ui/button.tsx"
      provides: "Button variants (primary, secondary, ghost) with Linear-style animations"
      exports: ["Button", "AnimatedButton", "buttonVariants"]
    - path: "website/lib/animation.ts"
      provides: "Bouncy spring preset for button interactions"
      contains: "springLinear"
  key_links:
    - from: "website/components/ui/button.tsx"
      to: "website/lib/animation.ts"
      via: "import springLinear preset"
      pattern: "springLinear"
---

<objective>
Redesign button component with Linear-style variants and bouncy spring animations.

Purpose: Buttons are the primary interactive element. Linear-style bouncy springs with proper hover/active states create the playful, premium feel that defines the v3.0 design system.

Output: Updated button.tsx with redesigned variants (primary gradient, secondary transparent, ghost) and proper size tokens (S/M/L), plus bouncy spring preset in animation.ts.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-component-library/30-CONTEXT.md
@.planning/phases/30-component-library/30-RESEARCH.md
@website/components/ui/button.tsx
@website/lib/animation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Linear bouncy spring preset to animation.ts</name>
  <files>website/lib/animation.ts</files>
  <action>
Add a new spring preset `springLinear` to animation.ts for Linear-style bouncy interactions:

```typescript
/**
 * LINEAR BOUNCY SPRING - Phase 30
 * Per CONTEXT.md: "Bouncy spring press feel - Linear-style slight overshoot, playful"
 * Higher stiffness (260) + moderate damping (20) creates slight overshoot
 */
export const springLinear = {
  type: "spring" as const,
  stiffness: 260,
  damping: 20,
};
```

This preset will be used by AnimatedButton for hover/tap animations.
  </action>
  <verify>File saves without TypeScript errors. `springLinear` export is present.</verify>
  <done>animation.ts exports `springLinear` with stiffness: 260, damping: 20</done>
</task>

<task type="auto">
  <name>Task 2: Redesign button variants and sizes</name>
  <files>website/components/ui/button.tsx</files>
  <action>
Update buttonVariants CVA definition with Linear-spec variants and sizes:

**Variant updates:**
1. `default` (primary): Add gradient background using `bg-gradient-to-r from-primary to-primary/90`. Keep existing hover state.

2. `secondary`: Change to transparent bg with border: `bg-transparent border border-white/20 text-foreground hover:bg-white/5`

3. `ghost`: Update to: `bg-transparent text-muted-foreground hover:text-foreground hover:bg-transparent` (no bg on hover, just text color change)

**Size updates per COMP-04 spec:**
- `sm`: `h-8 px-4 py-2 text-sm` (32px height, 8px vertical, 16px horizontal)
- `default`: `h-10 px-6 py-3 text-base` (40px height, 12px vertical, 24px horizontal)
- `lg`: `h-12 px-8 py-4 text-base` (48px height, 16px vertical, 32px horizontal)

Note: Keep existing `icon` size variant as-is.

**AnimatedButton updates:**
1. Import `springLinear` from animation.ts
2. Update `whileHover` to: `{ y: -2 }` (-2px hover lift per COMP-01)
3. Update `whileTap` to: `{ scale: 0.95 }` (0.95 active scale per COMP-01)
4. Change `transition` to use `springLinear` for bouncy feel

Preserve loading state behavior (spinner replaces text, width stays constant) per CONTEXT.md decision.
  </action>
  <verify>
Run `npm run build` in website directory - no TypeScript errors.
Visually inspect button in dev mode:
- Primary: gradient visible, -2px lift on hover, 0.95 scale on click with slight bounce-back
- Secondary: transparent with border, subtle bg on hover
- Ghost: no bg, muted text becomes white on hover
- Sizes: sm=32px, default=40px, lg=48px height
  </verify>
  <done>
Button variants match Linear spec:
- Primary has gradient, -2px hover lift, 0.95 active scale with bouncy spring
- Secondary is transparent with 20% white border, 5% bg on hover
- Ghost has no bg, muted->white text on hover
- Sizes: S=32px, M=40px, L=48px
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes in website directory
2. AnimatedButton uses springLinear preset from animation.ts
3. Hover lift is exactly -2px (y: -2)
4. Tap scale is exactly 0.95
5. Button sizes match: sm=32px (h-8), default=40px (h-10), lg=48px (h-12)
</verification>

<success_criteria>
- COMP-01: Primary button has gradient bg, -2px hover lift, 0.95 active scale, bouncy easing
- COMP-02: Secondary button has transparent bg, 20% white border, 5% bg on hover
- COMP-03: Ghost button has no bg, muted text, white on hover
- COMP-04: Button sizes S(32px), M(40px), L(48px) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-01-SUMMARY.md`
</output>
