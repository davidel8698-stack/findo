---
phase: 30-component-library
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/hero/GlassNav.tsx
autonomous: true

must_haves:
  truths:
    - "Navigation has 64px height (h-16)"
    - "Navigation is sticky with fixed positioning"
    - "Navigation is transparent at top of page"
    - "Navigation transitions to 85% opacity + stronger blur when scrolled"
    - "Navigation height stays consistent throughout scroll (no compacting)"
  artifacts:
    - path: "website/components/sections/hero/GlassNav.tsx"
      provides: "Navigation component with scroll state transitions"
      exports: ["GlassNav"]
  key_links:
    - from: "website/components/sections/hero/GlassNav.tsx"
      to: "window.scrollY"
      via: "scroll event listener"
      pattern: "scrollY > 50"
---

<objective>
Redesign navigation with proper scroll state transitions per Linear specifications.

Purpose: Navigation is the persistent UI element users see throughout the page. Proper scroll-state transitions (transparent -> glass) create a polished, professional experience without being distracting.

Output: Updated GlassNav.tsx with 64px height, 85% opacity + stronger blur when scrolled, consistent height throughout.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-component-library/30-CONTEXT.md
@.planning/phases/30-component-library/30-RESEARCH.md
@website/components/sections/hero/GlassNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GlassNav scroll state styling</name>
  <files>website/components/sections/hero/GlassNav.tsx</files>
  <action>
Update GlassNav component to match Linear specifications:

1. **Height**: Change to `h-16` (64px per COMP-08). Height stays consistent throughout scroll.

2. **Scroll threshold**: Keep at 50px (per CONTEXT.md: "50-100px scroll").

3. **Transparent state** (not scrolled):
   - `bg-transparent`
   - `border-b border-transparent`

4. **Glass state** (scrolled) - update to COMP-09 specs:
   - Mobile fallback: `bg-[rgb(24_24_27/0.85)]` (85% opacity)
   - Desktop glass:
     - `bg-[rgb(24_24_27/0.15)]` (lighter base for glass)
     - `backdrop-blur-[16px]` (stronger blur, up from 10px)
     - `border-white/10` (subtle border)

5. **Transition**: Keep `transition-all duration-300 ease-out` for smooth appearance.

Updated scrolled state classes:
```typescript
isScrolled
  ? cn(
      // 85% opacity mobile fallback (solid)
      "bg-[rgb(24_24_27/0.85)] border-b border-white/10",
      // Desktop: lighter bg + stronger 16px blur
      "md:supports-[backdrop-filter:blur(1px)]:bg-[rgb(24_24_27/0.15)]",
      "md:supports-[backdrop-filter:blur(1px)]:backdrop-blur-[16px]",
      "md:supports-[backdrop-filter:blur(1px)]:border-white/10"
    )
  : "bg-transparent border-b border-transparent"
```
  </action>
  <verify>
Run dev server, scroll page:
1. Nav starts transparent
2. After 50px scroll, nav transitions to glass state
3. Nav height remains 64px throughout
4. Glass has visible blur effect on desktop
5. Mobile shows solid 85% opacity background
  </verify>
  <done>
GlassNav matches Linear spec:
- 64px height (h-16)
- Transparent at top, glass after 50px scroll
- 85% opacity + 16px blur when scrolled
- Height consistent (no compacting)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes in website directory
2. Nav height is exactly 64px (h-16)
3. Nav is transparent when scrollY < 50
4. Nav shows 85% opacity background when scrollY >= 50
5. Desktop blur is 16px (stronger than before)
6. Height remains 64px at all scroll positions
</verification>

<success_criteria>
- COMP-08: Navigation has 64px height, sticky position, semi-transparent + blur on scroll
- COMP-09: Navigation shows 85% opacity + stronger blur (16px) when scrolled
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-03-SUMMARY.md`
</output>
