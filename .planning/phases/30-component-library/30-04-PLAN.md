---
phase: 30-component-library
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - website/components/sections/hero/HeroContent.tsx
  - website/components/sections/hero/TrustSignal.tsx
  - website/components/molecules/SocialProofRow.tsx
autonomous: true

must_haves:
  truths:
    - "Hero follows badge -> H1 -> subheadline -> CTAs -> social proof structure"
    - "Badge appears above H1 headline (small chip)"
    - "CTA pairing is Primary + Ghost buttons"
    - "Social proof row shows grayscale logos with 48px gap"
    - "Social proof logos are 60% opacity, become colored on hover"
  artifacts:
    - path: "website/components/sections/hero/HeroContent.tsx"
      provides: "Hero content with badge-first structure"
      exports: ["HeroContent"]
    - path: "website/components/molecules/SocialProofRow.tsx"
      provides: "Grayscale logo row for hero social proof"
      exports: ["SocialProofRow"]
  key_links:
    - from: "website/components/sections/hero/HeroContent.tsx"
      to: "website/components/ui/badge.tsx"
      via: "Badge import for hero badge"
      pattern: "import.*Badge"
    - from: "website/components/sections/hero/HeroContent.tsx"
      to: "website/components/molecules/SocialProofRow.tsx"
      via: "SocialProofRow import"
      pattern: "SocialProofRow"
---

<objective>
Implement Linear-style hero pattern structure with badge, headline, CTAs, and social proof row.

Purpose: The hero is the first impression. Linear's badge -> H1 -> subheadline -> CTAs -> social proof structure creates a clear visual hierarchy and immediate credibility.

Output: Updated HeroContent.tsx with badge above H1, proper CTA pairing, and new SocialProofRow molecule for grayscale logos.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-component-library/30-CONTEXT.md
@.planning/phases/30-component-library/30-RESEARCH.md
@website/components/sections/hero/HeroContent.tsx
@website/components/sections/hero/TrustSignal.tsx
@website/components/ui/badge.tsx
@website/components/molecules/CTAGroup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SocialProofRow molecule</name>
  <files>website/components/molecules/SocialProofRow.tsx</files>
  <action>
Create a new molecule for displaying grayscale customer logos (COMP-12):

```typescript
"use client";

import { cn } from "@/lib/utils";

interface Logo {
  src: string;
  alt: string;
}

interface SocialProofRowProps {
  logos: Logo[];
  className?: string;
}

/**
 * SocialProofRow - Grayscale customer logos per COMP-12
 *
 * Displays 4-6 business logos in grayscale with 60% opacity.
 * On hover, logos become full color and 100% opacity.
 * Gap between logos is 48px per Linear spec.
 */
export function SocialProofRow({ logos, className }: SocialProofRowProps) {
  return (
    <div
      className={cn(
        "flex items-center justify-center flex-wrap",
        "gap-12", // 48px gap per COMP-12
        className
      )}
    >
      {logos.map((logo) => (
        <img
          key={logo.alt}
          src={logo.src}
          alt={logo.alt}
          className={cn(
            "h-8 max-w-[100px] object-contain",
            "grayscale opacity-60",
            "hover:grayscale-0 hover:opacity-100",
            "transition-all duration-300"
          )}
        />
      ))}
    </div>
  );
}
```

Also export from molecules index.ts.
  </action>
  <verify>Component file creates without TypeScript errors. Export added to molecules/index.ts.</verify>
  <done>SocialProofRow molecule created with grayscale logos, 48px gap, 60% opacity, hover-to-color effect</done>
</task>

<task type="auto">
  <name>Task 2: Update HeroContent with badge-first structure</name>
  <files>website/components/sections/hero/HeroContent.tsx</files>
  <action>
Restructure HeroContent to follow Linear hero pattern (COMP-11):

1. **Add badge above H1**: Import Badge from ui/badge and add before headline:
```typescript
{/* Badge above headline per COMP-11 */}
<Badge variant="outline" className="mb-4">
  <span className="me-1">&#10024;</span> {/* Sparkle emoji */}
  חדש: אוטומציה מלאה
</Badge>
```

2. **Keep headline (H1)**: Already in place, just ensure proper spacing after badge.

3. **Keep subheadline**: Already in place.

4. **Update CTA section**: CTAGroup already handles Primary + Ghost pairing. Verify:
   - Primary button is solid/gradient
   - Secondary uses ghost variant (per CONTEXT.md: "CTA pairing: Primary + Ghost")

5. **Add SocialProofRow below CTAs**: Replace or augment TrustSignal with SocialProofRow:
```typescript
{/* Social proof logos per COMP-12 */}
<SocialProofRow
  logos={[
    { src: "/logos/business-1.svg", alt: "לקוח 1" },
    { src: "/logos/business-2.svg", alt: "לקוח 2" },
    { src: "/logos/business-3.svg", alt: "לקוח 3" },
    { src: "/logos/business-4.svg", alt: "לקוח 4" },
  ]}
  className="mt-8"
/>
```

Note: Keep existing TrustSignal as fallback since logo images may not exist yet. SocialProofRow can be placeholder-ready.

Final structure order:
1. Badge (new)
2. H1 headline
3. Subheadline
4. CTAs + trust text
5. Social proof (TrustSignal or SocialProofRow)
  </action>
  <verify>
Run dev server:
1. Badge appears above H1 (small pill shape)
2. H1 headline follows badge
3. Subheadline follows H1
4. CTAs (Primary + Ghost) follow subheadline
5. Social proof section appears below CTAs
  </verify>
  <done>
HeroContent follows Linear pattern:
- Badge above H1
- H1 -> subheadline -> CTAs -> social proof structure
- CTA pairing is Primary + Ghost
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes in website directory
2. Hero structure: Badge -> H1 -> subheadline -> CTAs -> social proof
3. Badge uses pill shape (rounded-full via badge.tsx)
4. SocialProofRow shows grayscale logos with 48px gap
5. Logo opacity is 60%, becomes 100% on hover
</verification>

<success_criteria>
- COMP-11: Hero follows badge -> H1 -> subheadline -> CTAs -> social proof structure
- COMP-12: Social proof row has grayscale logos, 48px gap, 60% opacity
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-04-SUMMARY.md`
</output>
