---
phase: 30-component-library
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/ui/badge.tsx
  - website/components/sections/FooterCTA.tsx
  - website/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Badges have pill shape with 20px border-radius"
    - "Badges have semantic color variants (success, warning, error, info)"
    - "Footer CTA section has clear tagline pattern"
    - "Footer CTA uses Primary + Ghost button pairing"
  artifacts:
    - path: "website/components/ui/badge.tsx"
      provides: "Badge with pill shape and semantic variants"
      exports: ["Badge", "badgeVariants"]
    - path: "website/components/sections/FooterCTA.tsx"
      provides: "Footer CTA section with tagline pattern"
      exports: ["FooterCTA"]
  key_links:
    - from: "website/app/page.tsx"
      to: "website/components/sections/FooterCTA.tsx"
      via: "FooterCTA import in page"
      pattern: "FooterCTA"
---

<objective>
Redesign badges with semantic variants and create footer CTA section with tagline pattern.

Purpose: Badges communicate status and categories throughout the UI. A dedicated footer CTA section with consistent tagline pattern provides a final conversion opportunity before users leave.

Output: Updated badge.tsx with pill shape (20px radius) and semantic color variants, plus new FooterCTA section component.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-component-library/30-CONTEXT.md
@.planning/phases/30-component-library/30-RESEARCH.md
@website/components/ui/badge.tsx
@website/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign badge with pill shape and semantic variants</name>
  <files>website/components/ui/badge.tsx</files>
  <action>
Update badgeVariants CVA definition per COMP-07:

1. **Base styles**: Change border-radius to `rounded-[20px]` for pill shape (20px radius).
   Keep existing: inline-flex, items-center, border, padding, font-semibold.

2. **Add semantic color variants** using status colors from COLOR-05:
```typescript
const badgeVariants = cva(
  "inline-flex items-center rounded-[20px] border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2",
  {
    variants: {
      variant: {
        default:
          "border-transparent bg-primary text-primary-foreground hover:bg-primary/80",
        secondary:
          "border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80",
        destructive:
          "border-transparent bg-destructive text-destructive-foreground hover:bg-destructive/80",
        outline: "text-foreground border-border",
        // Semantic status variants per COLOR-05
        success:
          "border-transparent bg-[#22C55E] text-white hover:bg-[#22C55E]/80",
        warning:
          "border-transparent bg-[#EAB308] text-black hover:bg-[#EAB308]/80",
        error:
          "border-transparent bg-[#EF4444] text-white hover:bg-[#EF4444]/80",
        info:
          "border-transparent bg-[#3B82F6] text-white hover:bg-[#3B82F6]/80",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
);
```

Note: Using hardcoded hex values since status colors are from the design spec (COLOR-05).
  </action>
  <verify>Badges render with pill shape (rounded-[20px]). Semantic variants (success, warning, error, info) display correct colors.</verify>
  <done>Badge has 20px pill radius and semantic color variants (success, warning, error, info)</done>
</task>

<task type="auto">
  <name>Task 2: Create FooterCTA section component</name>
  <files>website/components/sections/FooterCTA.tsx</files>
  <action>
Create a new footer CTA section per COMP-10 with tagline pattern:

```typescript
"use client";

import { ArrowLeft } from "lucide-react";
import { AnimatedButton } from "@/components/ui/button";
import { SectionReveal, SectionRevealItem } from "@/components/motion/SectionReveal";
import { cn } from "@/lib/utils";

interface FooterCTAProps {
  className?: string;
}

/**
 * FooterCTA - Final conversion section with tagline pattern
 *
 * Per COMP-10: Footer CTA section with clear tagline pattern
 * Structure: Tagline -> Subtext -> Primary + Ghost CTAs
 */
export function FooterCTA({ className }: FooterCTAProps) {
  return (
    <section
      className={cn(
        "py-section-cta",
        "text-center",
        className
      )}
    >
      <div className="container mx-auto px-4">
        <SectionReveal>
          <SectionRevealItem>
            {/* Tagline - bold, memorable */}
            <h2
              className={cn(
                "text-3xl md:text-4xl lg:text-5xl",
                "font-bold",
                "mb-4",
                "text-gradient-brand",
                "text-shadow-[0_0_15px_rgba(249,115,22,0.35)]"
              )}
            >
              מוכן להפוך את העסק לאוטומטי?
            </h2>
          </SectionRevealItem>

          <SectionRevealItem>
            {/* Subtext */}
            <p className="text-lg text-muted-foreground mb-8 max-w-xl mx-auto">
              הצטרף ל-573+ עסקים שכבר חוסכים שעות כל יום עם Findo
            </p>
          </SectionRevealItem>

          <SectionRevealItem>
            {/* CTA buttons - Primary + Ghost pairing */}
            <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
              <AnimatedButton size="lg" variant="default" asChild>
                <a href="#signup">
                  התחל בחינם
                  <ArrowLeft className="ms-2 h-4 w-4" />
                </a>
              </AnimatedButton>

              <AnimatedButton size="lg" variant="ghost" asChild>
                <a href="#contact">
                  דבר עם הצוות
                </a>
              </AnimatedButton>
            </div>
          </SectionRevealItem>

          <SectionRevealItem>
            {/* Trust reassurance */}
            <p className="text-sm text-muted-foreground mt-6">
              ללא כרטיס אשראי • ביטול בכל עת
            </p>
          </SectionRevealItem>
        </SectionReveal>
      </div>
    </section>
  );
}
```

Export from sections index if one exists, or ensure direct import works.
  </action>
  <verify>FooterCTA renders with tagline, subtext, Primary + Ghost buttons, trust text.</verify>
  <done>FooterCTA section created with tagline pattern and Primary + Ghost CTA pairing</done>
</task>

<task type="auto">
  <name>Task 3: Integrate FooterCTA into page.tsx</name>
  <files>website/app/page.tsx</files>
  <action>
Add FooterCTA component to page.tsx, replacing or augmenting the existing footer section:

1. Import FooterCTA:
```typescript
import { FooterCTA } from "@/components/sections/FooterCTA";
```

2. Add FooterCTA before the final footer guarantee section (around line 209):
```typescript
{/* Footer CTA - Final conversion opportunity (COMP-10) */}
<FooterCTA className="bg-muted/30" />

{/* Footer Guarantee Badge */}
<section className={cn("py-section-footer pb-20 md:pb-12")}>
  ...
</section>
```

This places a dedicated CTA section with tagline pattern before the guarantee badges.
  </action>
  <verify>Page renders with FooterCTA section visible above footer guarantee badges.</verify>
  <done>FooterCTA integrated into page.tsx before footer guarantee section</done>
</task>

</tasks>

<verification>
1. `npm run build` passes in website directory
2. Badges have pill shape (rounded-[20px])
3. Badge semantic variants render correct colors (green success, yellow warning, red error, blue info)
4. FooterCTA section displays tagline pattern
5. FooterCTA has Primary + Ghost button pairing
</verification>

<success_criteria>
- COMP-07: Badges have pill shape (20px radius) and semantic color variants
- COMP-10: Footer CTA section with tagline pattern implemented
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-05-SUMMARY.md`
</output>
