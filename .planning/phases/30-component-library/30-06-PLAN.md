---
phase: 30-component-library
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - website/app/layout.tsx
  - website/components/navigation/Navigation.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Navigation is sticky with scroll state transitions (opacity + blur)"
    - "Navigation shows 85% opacity + 16px blur when scrolled"
    - "Logo and navigation links visible in the header"
  artifacts:
    - path: "website/components/navigation/Navigation.tsx"
      provides: "Navigation content (logo + links) wrapped in GlassNav"
      min_lines: 30
    - path: "website/app/layout.tsx"
      provides: "Navigation rendered in root layout"
      contains: "Navigation"
  key_links:
    - from: "website/components/navigation/Navigation.tsx"
      to: "website/components/sections/hero/GlassNav.tsx"
      via: "import GlassNav"
      pattern: "import.*GlassNav"
    - from: "website/app/layout.tsx"
      to: "website/components/navigation/Navigation.tsx"
      via: "import Navigation"
      pattern: "import.*Navigation"
---

<objective>
Integrate GlassNav component into the application layout.

Purpose: GlassNav exists (64 lines, fully implemented) but is orphaned - never imported or rendered. Users cannot see the sticky navigation with scroll state transitions (Phase 30 success criterion #3).

Output: Navigation visible in header with logo and scroll-triggered glass effect.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing component to integrate
@website/components/sections/hero/GlassNav.tsx

# Target file for rendering
@website/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navigation component with GlassNav wrapper</name>
  <files>website/components/navigation/Navigation.tsx</files>
  <action>
    Create a new Navigation component that:
    1. Imports GlassNav from "@/components/sections/hero/GlassNav"
    2. Import Button from "@/components/ui/button"
    3. Renders GlassNav as the wrapper
    4. Contains a container div with flex layout
    5. Includes:
       - Findo logo (text-based: "Findo" in bold, white text)
       - Simple navigation links using Button variant="ghost" (e.g., "התחל בחינם")
    6. Uses RTL-appropriate spacing (justify-between, items-center)
    7. Keeps content minimal - just logo and CTA for now

    Why text logo: No image assets needed, matches minimalist Linear aesthetic
    Why single CTA: Homepage is conversion-focused, no complex nav needed
  </action>
  <verify>
    - File exists at website/components/navigation/Navigation.tsx
    - Imports GlassNav from correct path
    - Exports Navigation component
    - Contains "Findo" logo text
  </verify>
  <done>Navigation component created with GlassNav wrapper and minimal content</done>
</task>

<task type="auto">
  <name>Task 2: Import and render Navigation in layout.tsx</name>
  <files>website/app/layout.tsx</files>
  <action>
    Modify layout.tsx to:
    1. Add import: `import { Navigation } from "@/components/navigation/Navigation"`
    2. Render Navigation component inside Providers, after BackgroundDepth
    3. Add a wrapper div with pt-16 (padding-top: 64px) around {children} to account for fixed nav height

    Exact structure inside Providers:
    ```tsx
    <Providers>
      <BackgroundDepth />
      <Navigation />
      <div className="pt-16">
        {children}
      </div>
    </Providers>
    ```

    This ensures nav is always visible and body content doesn't overlap with the fixed 64px nav.
  </action>
  <verify>
    - layout.tsx imports Navigation
    - Navigation rendered in correct position
    - Body has padding-top to account for fixed nav
    - `npm run build` succeeds
  </verify>
  <done>Navigation integrated into root layout, visible on all pages</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should succeed without errors
2. Run dev server and observe:
   - Navigation visible at top of page
   - Findo logo visible
   - On scroll past 50px, glass effect activates (opacity + blur)
3. Verify no layout shift - content starts below 64px nav height
</verification>

<success_criteria>
- GlassNav component is actively rendered (no longer orphaned)
- Navigation shows transparent state at top
- Navigation shows glass effect (85% opacity + 16px blur) after scrolling 50px
- Page content does not overlap with fixed navigation
- COMP-08 and COMP-09 requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-06-SUMMARY.md`
</output>
