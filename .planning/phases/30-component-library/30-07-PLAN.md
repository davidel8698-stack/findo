---
phase: 30-component-library
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/offer/ZeroRiskSummary.tsx
  - website/components/sections/social-proof/TrustBadges.tsx
  - website/app/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Card specialized variants are integrated and functional"
    - "AnimatedCard or GradientBorderCard used in visible sections"
    - "Premium card effects visible to users"
  artifacts:
    - path: "website/components/sections/offer/ZeroRiskSummary.tsx"
      provides: "Uses AnimatedCard for hover lift effect"
      contains: "AnimatedCard"
    - path: "website/components/sections/social-proof/TrustBadges.tsx"
      provides: "Uses GradientBorderCard for premium appearance"
      contains: "GradientBorderCard"
  key_links:
    - from: "website/components/sections/offer/ZeroRiskSummary.tsx"
      to: "website/components/ui/card.tsx"
      via: "import AnimatedCard"
      pattern: "import.*AnimatedCard.*from.*card"
    - from: "website/components/sections/social-proof/TrustBadges.tsx"
      to: "website/components/ui/card.tsx"
      via: "import GradientBorderCard"
      pattern: "import.*GradientBorderCard.*from.*card"
---

<objective>
Integrate advanced card variants (AnimatedCard, GradientBorderCard) into visible sections.

Purpose: GradientBorderCard, AnimatedCard exist in card.tsx but have zero usage outside the file itself. Users cannot see the premium card effects that were built in Phase 30-02.

Output: Card variants actively used in ZeroRiskSummary and TrustBadges sections with visible hover and gradient effects.
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Available card variants
@website/components/ui/card.tsx

# Target files for integration
@website/components/sections/offer/ZeroRiskSummary.tsx
@website/components/sections/social-proof/TrustBadges.tsx
@website/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use AnimatedCard in ZeroRiskSummary</name>
  <files>website/components/sections/offer/ZeroRiskSummary.tsx</files>
  <action>
    Update ZeroRiskSummary.tsx to use AnimatedCard:
    1. Add import: `import { AnimatedCard } from "@/components/ui/card"`
    2. Replace the outer div (line ~40: `<div className={cn("bg-card border rounded-xl...")}>`
       with `<AnimatedCard className={cn("shadow-sm w-full max-w-lg mx-auto")}>`
    3. Remove redundant classes that AnimatedCard already provides:
       - Remove: bg-card, border, rounded-xl, p-6 md:p-8 (AnimatedCard has rounded-2xl, p-8)
       - Keep: shadow-sm, w-full, max-w-lg, mx-auto

    Why AnimatedCard: Provides hover lift animation (-4px y-translate) and rim lighting,
    making the zero-risk summary feel more premium and interactive.
  </action>
  <verify>
    - ZeroRiskSummary imports AnimatedCard
    - Uses AnimatedCard instead of plain div
    - Component renders without errors
    - Hover over component shows -4px lift animation
  </verify>
  <done>ZeroRiskSummary uses AnimatedCard with visible hover effect</done>
</task>

<task type="auto">
  <name>Task 2: Use GradientBorderCard wrapper in TrustBadges</name>
  <files>website/components/sections/social-proof/TrustBadges.tsx</files>
  <action>
    Update TrustBadges.tsx to use GradientBorderCard as an optional wrapper:
    1. Add import: `import { GradientBorderCard } from "@/components/ui/card"`
    2. Add prop: `withCard?: boolean` (default false to maintain backward compatibility)
    3. When withCard is true, wrap the badges container in GradientBorderCard:
       ```tsx
       const content = (
         <div className={cn("flex flex-wrap justify-center...", className)}>
           {badges.map(...)}
         </div>
       );

       if (withCard) {
         return <GradientBorderCard gradient="subtle">{content}</GradientBorderCard>;
       }
       return content;
       ```
    4. This allows sections to opt-in to the gradient border effect

    Why optional: TrustBadges is used in multiple places, some may not want the card wrapper.
  </action>
  <verify>
    - TrustBadges imports GradientBorderCard
    - withCard prop exists with false default
    - When withCard=true, GradientBorderCard wraps content
    - Backward compatible - existing usages unchanged
  </verify>
  <done>TrustBadges supports GradientBorderCard wrapper via withCard prop</done>
</task>

<task type="auto">
  <name>Task 3: Enable withCard on TrustBadges in page.tsx</name>
  <files>website/app/page.tsx</files>
  <action>
    Update the TrustBadges usage in page.tsx to enable the gradient border:
    1. Find line ~164: `<TrustBadges />`
    2. Add withCard prop: `<TrustBadges withCard />`

    This makes the GradientBorderCard effect visible to users on the main page.
  </action>
  <verify>
    - page.tsx has TrustBadges with withCard prop
    - `npm run build` succeeds
    - TrustBadges shows gradient border on the page
  </verify>
  <done>GradientBorderCard visible on main page via TrustBadges withCard</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should succeed without errors
2. Run dev server and test:
   - ZeroRiskSummary: Hover shows -4px lift animation
   - TrustBadges on main page: Shows gradient border (withCard enabled)
3. Verify existing page renders correctly (no regression)
</verification>

<success_criteria>
- AnimatedCard used in ZeroRiskSummary with visible hover effect
- GradientBorderCard visible on main page via TrustBadges withCard prop
- Card variants actively rendered and visible to users (no longer orphaned)
- COMP-06 (card variants) requirement addressed
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-07-SUMMARY.md`
</output>
