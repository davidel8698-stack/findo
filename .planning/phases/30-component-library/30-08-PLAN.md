---
phase: 30-component-library
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - website/components/sections/offer/GuaranteeBadges.tsx
  - website/components/sections/hero/HeroContent.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Badge semantic variants (success/warning/error/info) are usable"
    - "Semantic badge colors visible in actual UI"
    - "COLOR-05 semantic status colors leveraged"
  artifacts:
    - path: "website/components/sections/offer/GuaranteeBadges.tsx"
      provides: "Uses Badge variant='success' for guarantee indicators"
      contains: 'variant="success"'
    - path: "website/components/sections/hero/HeroContent.tsx"
      provides: "Uses Badge variant='info' for feature announcements"
      contains: 'variant="info"'
  key_links:
    - from: "website/components/sections/offer/GuaranteeBadges.tsx"
      to: "website/components/ui/badge.tsx"
      via: "import Badge with success variant"
      pattern: 'Badge.*variant.*success'
    - from: "website/components/sections/hero/HeroContent.tsx"
      to: "website/components/ui/badge.tsx"
      via: "import Badge with info variant"
      pattern: 'Badge.*variant.*info'
---

<objective>
Integrate semantic badge variants (success/warning/error/info) into visible sections.

Purpose: Badge semantic variants were implemented per COLOR-05 spec but have zero usage. The success/warning/error/info colors exist in badge.tsx but users never see them.

Output: Semantic badge variants actively used in GuaranteeBadges (success) and HeroContent (info).
</objective>

<execution_context>
@C:\Users\דודאלמועלם\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\דודאלמועלם\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Badge component with semantic variants
@website/components/ui/badge.tsx

# Target files for integration
@website/components/sections/offer/GuaranteeBadges.tsx
@website/components/sections/hero/HeroContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use success badge in GuaranteeBadges</name>
  <files>website/components/sections/offer/GuaranteeBadges.tsx</files>
  <action>
    Update GuaranteeBadges.tsx to use Badge component with success variant:
    1. Check if Badge is already imported, if not add: `import { Badge } from "@/components/ui/badge"`
    2. Find the guarantee labels/text elements
    3. For each guarantee item, add a small Badge with variant="success" to indicate "verified" status
    4. Example: Wrap guarantee text like "30 יום החזר כספי" with a success badge prefix:
       ```tsx
       <Badge variant="success" className="me-2">מאומת</Badge>
       ```
       Or add a checkmark-style success badge next to each guarantee
    5. Keep it subtle - the success green (#22C55E) should complement, not overwhelm

    Why success variant: Guarantees represent verified/confirmed promises - success semantic color reinforces trust.
  </action>
  <verify>
    - GuaranteeBadges imports Badge
    - At least one Badge with variant="success" rendered
    - Green (#22C55E) success color visible in UI
    - Component renders without errors
  </verify>
  <done>GuaranteeBadges uses success badge variant for verified guarantees</done>
</task>

<task type="auto">
  <name>Task 2: Use info badge in HeroContent announcement</name>
  <files>website/components/sections/hero/HeroContent.tsx</files>
  <action>
    Update HeroContent.tsx to use info badge variant for feature announcement:
    1. Locate the existing Badge usage (should be variant="outline" per Phase 30-04)
    2. Consider changing to variant="info" for the tagline badge OR
    3. Add a secondary announcement badge with variant="info" for a "new feature" or "beta" indicator
    4. If the primary badge should stay outline (brand consistency), add a small info badge:
       ```tsx
       <div className="flex gap-2 items-center justify-center mb-4">
         <Badge variant="info">חדש</Badge>
         <Badge variant="outline">Findo AI מופעל</Badge>
       </div>
       ```
    5. The info blue (#3B82F6) should indicate something new/informational

    Why info variant: Announcements and new features naturally fit the "informational" semantic meaning.
    Note: Keep the existing outline badge if it's working well - add info as complementary.
  </action>
  <verify>
    - HeroContent has Badge with variant="info"
    - Blue (#3B82F6) info color visible in hero section
    - Existing outline badge still works if kept
    - Layout accommodates multiple badges gracefully
  </verify>
  <done>HeroContent uses info badge variant for feature announcement</done>
</task>

</tasks>

<verification>
1. Run `npm run build` - should succeed without errors
2. Run dev server and verify:
   - GuaranteeBadges shows green success badges
   - HeroContent shows blue info badge
3. Colors match COLOR-05 spec: success=#22C55E, info=#3B82F6
4. Badges are readable (proper contrast with text colors)
</verification>

<success_criteria>
- Badge variant="success" used in visible section (GuaranteeBadges)
- Badge variant="info" used in visible section (HeroContent)
- Semantic colors from COLOR-05 spec actively displayed to users
- COMP-07 (semantic badge variants) requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/30-component-library/30-08-SUMMARY.md`
</output>
